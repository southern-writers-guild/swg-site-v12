# SWG Project Log

Running record of decisions, thinking-in-progress, and open threads that don't belong in `CLAUDE.md` (technical/operational instructions) but shouldn't be lost either. Newest entries at the top. Each entry is dated. Anything marked **Open** is not a decision — treat it as where the thinking stood, not as settled ground.

Read this at the start of any SWG session, same as `CLAUDE.md`.

---

## 2026-09-10 — Grace's Sanity invite: found genuinely missing, blocked from here, handed to Claude Code

Rick asked for an invite to Sanity for Grace. Checked live membership first (`sanity users list --invitations`) rather than trusting the 2026-09-04 log line that already flagged this open: real result is she's not there at all, not a member, not a pending invite -- whatever went out around 2026-08-29 is gone, never turned into anything she could act on. MJ is in, confirmed active, Administrator, showing as "Mikie Pdizzle."

**Real correction caught mid-task, worth remembering:** first draft of this used `gray@southernwritersguild.com`, matching the Guild-branded address pattern from the 2026-08-28 "Going Pro" plan (step 4: each founder signs into everything with a dedicated `@southernwritersguild.com` Google account). Rick caught this before it went out -- the actual, agreed-on pattern is each founder's own existing personal Google login, not a new Guild-branded account. MJ showing as "Mikie Pdizzle" (not "hank" or "MJ Polk") is consistent with this being how MJ's own invite already worked, not a new decision. **The Going Pro plan's step 4 is stale against what's actually happening and should be revisited, not assumed correct, next time it comes up.** Grace's real address, confirmed by Rick directly: `turtlemeatpodcast@gmail.com`.

**Whichever Claude Code session picks this up next, per Rick's explicit request -- do this, don't re-derive a plan:**

Run:
```
sanity users invite turtlemeatpodcast@gmail.com --role administrator --project-id fe6l0kiy
```

Same "treat all three as equal" call already on record (2026-08-29 (3) entry, further down this file) -- Administrator, not a lesser role.

**Why this needed a handoff at all, worth remembering:** this Cowork session has a Sanity CLI passthrough (`run_sanity_cli`), but it's deliberately restricted to a safe subset of commands -- `users invite` isn't in it, confirmed by actually trying it, not assumed. No documented public API endpoint for email-based invites either (checked the `projects-api` and `roles` OpenAPI specs directly -- both only cover role changes for users who already exist as members, nothing for inviting a new one by email). First instinct was to offer Rick the sanity.io/manage click-path instead, framed as the easier of two options -- that was wrong. See the new line in CLAUDE.md's "Never send him hunting through a UI" rule: Claude Code is the default for anything blocked here, not a UI walkthrough offered alongside it.

---

## 2026-09-09 — First reported timeout since the retry fix; billing ruled out

Rick's first pull this morning failed. Asked to check whether it was Google billing — he'd just sent a prepayment after a "frankly suspicious looking" letter from Google turned out to be real.

Tested live (Cowork's built-in browser, real requests against the production `/api/tarot` endpoint, not guessed): both engines came back clean, text in 4s, image in 13s — no error, no rate limit, nothing billing-shaped. A real billing lockout doesn't clear itself overnight, so if it were that, it would still be failing now. It isn't. Couldn't pull the actual error line from the failed attempt itself (needs a Vercel dashboard login, not done on Rick's behalf), but a clean pass right now is real evidence against anything systemic.

**Real signal worth keeping:** Rick's own framing — first timeout he's seen *since* the silent-retry fix shipped (`b845b25`, 2026-09-08). Consistent with the known ~5-7% single-attempt failure rate from the time-budget architecture (see the 2026-09-08 entry below) rather than a new regression — one miss on its own isn't enough to call anything broken, but worth tracking if a second one shows up soon after.

Reading counter (see entry below) sat at 5 by the time this was checked, including one live test call from this session — first real confirmation the counter is tracking actual traffic, not just the manual test pulls from yesterday.

---

## 2026-09-08 (later same day) — reading counter added; Vercel invocation count checked as a stopgap

Rick asked whether there's any way to see how many pulls the tarot feature has gotten. Checked first, honestly: no analytics existed anywhere in the codebase (grepped clean). Vercel's dashboard invocation count (28 today, Rick's own testing) was offered as a rough zero-build stopgap while the real thing got built.

**Built the real thing (`2f5d9a5`).** A singleton Sanity document, `tarotStats` (`_id: "tarotStats"`, field `readingCount`), created and published directly via Sanity today, seeded at 0. `api/tarot.js` now bumps it by one on every successful text-engine completion — one bump per reading, not per engine call, since text and image always fire together per submission and double-counting would be worse than not counting. The write is fire-and-forget (never awaited, wrapped so a Sanity hiccup or a missing token can never fail or slow down a reading) and runs through a brand-new write-scoped client, kept deliberately separate from the existing untokened read client and from `SANITY_PREVIEW_TOKEN` (that one's scoped to preview/draft reads, not writes — mixing the two would be exactly the kind of key-scope confusion Rick's already had enough of).

**No Studio schema change.** `list_workspace_schemas` showed this project's schema is Studio-deployed from a separate repo, not local to `swg-site` and not MCP-managed — so a formal `tarotStats` schema type would mean finding and touching that other Studio project. Skipped entirely: `tarotStats` needs no editorial UI, since Rick can just ask for the current count directly in chat and get a live Sanity query back, no dashboard involved.

**Done, same day, closed out.** Rick initially declined outright ("i dont do API codes") when the plan was Vercel's website dashboard — real, valid objection, not stubbornness: he'd fought that exact multi-tab, wrong-environment-checkbox flow before and it always ate an hour. He then remembered `SANITY_PREVIEW_TOKEN` (this repo's private-preview build, a few entries further down this file) got set up without that fight, and asked for the same approach here: a Claude Code terminal session, not the website.

That handoff worked. Claude Code pushed the seven commits that had been sitting local, walked Rick through creating a Sanity token (`sanity.io/manage` → project `fe6l0kiy` → API → Tokens → Add API token, name `tarot-counter`, permission Editor, no expiration), set it as `SANITY_WRITE_TOKEN` in Vercel via `vercel env add` — one terminal prompt, one paste — and while testing, also tightened the write itself (`cd3add5`): now awaited and confirmed rather than fired blind, since fire-and-forget made a live test hard to trust. Confirmed for real, not assumed: queried `tarotStats.readingCount` directly and watched it move off 0 after a live test pull.

**Interface decision, asked directly and answered:** no dedicated page for the count — offered to build a small bookmarkable one-number page, Rick said "ask me is fine." So the interface is: ask Claude (Cowork or otherwise), get a live query back. Don't build a counter page later unless he actually asks for one.

---

## 2026-09-08 — Tarot image failure rate: found the real mechanism, shipped the cheap fix, logged the real one

Rick's actual complaint: too many readings dead-end on the first try (~5-7% by his own estimate after we talked through it, down from his first guess of ~20%). Asked to think it through properly rather than patch blind.

**The mechanism, not a guess.** Traced the actual numbers in `api/tarot.js`: `MAX_IMAGE_ATTEMPTS` is 6, but that's fictional under real Gemini latency. `GEMINI_TIMEOUT_MS` is 90s, `WORST_CASE_ATTEMPT_MS` is 125s, `ENGINE_TIME_BUDGET_MS` is 260s (20s under Vercel's 280s hard kill). The loop refuses to start another attempt unless a full 125s of budget remains, and real Gemini calls regularly run 60-90s+ (per the comment already in the file from an earlier fix). Net effect: most readings only ever get 1-2 real content-quality attempts, not 6. Worse, `callGeminiWithFastRetry` can silently fire a *second* Gemini call inside what the outer loop still counts as attempt 1 (when Gemini returns no image at all) -- meaning one unlucky moment can burn most of the 260s budget before a single quality check has run.

**Diagnosis Rick asked for directly: are we bolting patches onto a rickety frame? Yes, specifically.** Every fix so far (raise the timeout, stop one bad attempt from crashing the whole loop, add the time-budget guard, add the fast retry) was a correct fix for a real bug, and none of them questioned whether a multi-step, vision-validated generation pipeline belongs inside one synchronous HTTP request bound by a hard platform timeout in the first place. It doesn't, really -- that mismatch is the actual root cause, not any single bug.

**Shipped today (`b845b25`), the cheap half of the fix.** The client now retries each engine call once, silently, before ever surfacing an error -- a failure the visitor never sees unless it happens twice in a row (per the math above, that should be rare). Paired with honest wait-time copy: the loading state previously implied an instant return; now it rotates through a few short in-voice status lines ("He's particular about what he shows you. Hang tight," etc.) and swaps to a distinct line once a retry is actually in flight, so a longer wait reads as Jean-Paul being particular, not the page being broken. Considered and deliberately skipped: a literal animated/dancing-peacock graphic Rick floated -- fun idea, but it cuts against the voice rules we spent all of yesterday tightening (never winks at what he is, no modern references, dead-straight ceremonial delivery) and would need real new art production. The rotating text gets the same "something's happening, stay with me" effect for free, in the voice that's already established.

**Not done, and shouldn't be done as a quick patch:** the real fix is decoupling generation from the request/response cycle -- run the job in the background, let the page poll or stream its status, and stop being bound by Vercel's hard wall-clock at all. That would let a slow Gemini day mean a longer wait instead of a dead end, and would let the code actually use something closer to its full attempt budget. This is a real infrastructure change (needs somewhere to track a job's status, a polling endpoint, a patient loading UI), not a code tweak -- worth deciding on deliberately when there's appetite for it, not bolting on today.

---

## 2026-09-07 (later same day) — repo-desync structural fix, tarot placeholder rework, image caricature drift, running observations list

**Repo-location trap strengthened (`0928c3d`).** The existing "wrong folder" warning only caught a no-git decoy folder. It didn't catch a session pointed at a different but *valid* clone of the same repo, where git looks completely normal (clean status, "everything up to date") while real commits made elsewhere are invisible to it -- which is what actually happened today (Claude Code reported nothing to push while 5 real commits sat unpushed). `CLAUDE.md` now requires every session to run `git rev-parse --show-toplevel` + `git log --oneline -1` as its first action and verify the exact full path, not just the folder name, before trusting anything git tells it.

**Tarot ghost-text placeholders reworked (`6326075`).** The per-category placeholder scheme (added `b633a2c`, same day) turned out broken: a question's Sanity `category` tag means "what this question is about," not "what shape a good answer takes," so a "what color should X be" question tagged `object` got a random noun placeholder that didn't read as an answer at all. Replaced with one universal pool of lines built to work as a plausible deflecting non-answer to nearly any question, so it can't drift out of sync as the question pool grows.

**Image caricature drift -- two rounds.** First round (`bc46dd6`): added an explicit caricature ban to the "comical" style register only, after a card showed exaggerated cartoon proportions. Recurred on a later card (King of Discerning Palates) that was also clearly comic in content. Realized the style register is picked at random *before* the model sees that day's answers, independent of whether the resulting scene is inherently funny -- so a wacky scene can land under the "painterly" register, which had zero caricature guidance. Second round (`2ce83f4`): added matching caricature-ban language to the painterly register too. Not yet confirmed fixed -- one recurrence after the first fix isn't enough data to call this closed; watch for a third instance.

**Tarot voice: over-explained titles.** "Eight of Feathered Profanity Tutor" was within the 2-5 word hard limit but still read as one word too long -- "Tutor" just re-explained what "Feathered Profanity" already implied. Added a rule (alongside the existing pun-avoidance rule) treating a trailing redundant role/occupation word as the same over-explaining failure, with the worked example baked in.

**Image voice doc: corrected a wrong note.** The doc's "OPEN QUESTION" note incorrectly implied a decision not to fix personalization bias was tied to protecting the reference images / the API key. Rick corrected this: the API key has been rotated more than once already for an unrelated reason (it leaked into code once) and that topic is closed; it was never about the reference images. Note rewritten to reflect that the actual mitigation is prompt-level (an explicit SUBJECT DIVERSITY rule against defaulting to one recurring elderly-farmer figure), not account-level.

**Working-mode note from Rick (2026-09-07):** when he flags something while testing, it does not need an immediate fix every time -- log the observation, act only when it's clearly warranted. This log is where that running list lives; check here for standing observations before assuming a clean slate.

**Open / watching, not yet acted on:**
- Caricature drift in card images -- two fixes shipped today, not yet confirmed to have fully resolved it.
- A second border-defect pattern (a uniform 4-sided white photo-mat that got through even the *original*, pre-fix all-four-edge border check) -- flagged once, not yet investigated.

---

## 2026-09-07 — Jean-Paul's Tarot: timeout root cause found and fixed, forbidden-scene collisions fixed at the source, court-rank reference sets added, voice instructions tightened

This session's real debugging work (git commits `2428585` through `9f1b2c8`, all 2026-09-07, confirmed on `origin/main`) was never logged here at the time — captured after the fact from git history and live-verified against the deployed code, not from memory.

**Timeouts — real cause found, not the first guess.** The retry budget had already been raised from 3 to 6 attempts, but timeouts kept happening. Root cause (`e583f90`): a single slow or failed Gemini call was crashing the *entire* retry loop, not just costing one attempt — so "6 attempts" was really only ever 1. Fixed by wrapping transport calls so a failure retries like a content rejection already did, raising the per-call timeout to 90s to match real observed latency (was cut to 40s; real generations regularly take 60-90s+), and adding a time-budget guard so a doomed attempt is never started only to get silently killed by Vercel's hard duration cap.

**Forbidden-scene collisions — fixed at the source, not just with more images (`706d6e2`).** The model was ignoring generic "pick something different" retry wording. Fixed by pre-scanning the raw answers themselves for keywords tied to each forbidden scene and handing the model a concrete, code-picked alternative from attempt 1, then a fresh targeted alternative on every retry after a real collision.

**Court-rank reference sets added (`24104cf`, confirmed live `9f1b2c8`).** Queen/King draws now reach a new mature/settled reference set (5 images); Page/Knight reach a new active/youthful set (2 images); everything else keeps the original 4 (default). Forbidden-scene collision detection extends to all 7 new images, scoped per set. Rank is now decided once, client-side, and shared with both engines (previously two independent serverless calls with no way to agree on it).

**Regression caught and restored (`bbb3303`).** The rare "name only, no rank" Major card (the voice document's 1-in-20 case) had been silently made impossible by the rank-in-code fix. Restored with a real code-level 1-in-20 roll (verified 5.0% over 1M local trials); on that roll, Claude gets a different prompt for a plain card name braiding all three answers, with the same noun-naming backstop plus a defensive strip against a rank word sneaking in.

**Image variety — style register randomized in code (`5d4d8be`), audited clean (`040f88b`/`daa8332`).** Tone was drifting to one register; now randomized in code. A real production batch of 10 came back 7 uncanny-painterly / 3 comical, consistent with the intended ~50/50 split at scale — not a bias. (The separately-documented "Google-account personalization bias" open question in `tarotVoiceImage`, re: recurring porch/rocking-chair motifs, is still unaddressed and is a more likely explanation for any remaining variety complaints than style register.)

**Voice instructions tightened same day, in Sanity (`tarotVoiceText`), not in this repo:** (1) added an explicit test for genericness/tepid readings, parallel to the existing never-name-the-answer test; (2) widened never-name-the-answer to cover numbers/quantities, not just nouns, and explicitly forbade the "[Rank] of [Suit]" naming syntax from leaking into reading prose; (3) added hard length limits — readings capped at 15-40 words, one to three sentences; Major/Court titles capped at two to five words, never a full sentence. Motivated by Rick's live testing same day (a card whose reading opened "Ten of somebody's kin..." — likely either a number-naming leak or a rank-syntax leak, both fixed since a single data point couldn't disambiguate; and readings/titles running noticeably longer than the calibration examples).

---


## 2026-09-06 (later) — Jean-Paul's Tarot, Phases 3-4: both engines built and hardened, first real page live

**Phase 3 — the two engines**, built as one shared serverless function (`api/tarot.js`, dispatched by `?engine=text|image`) rather than two separate files, since the project sits at Vercel's Hobby-plan 12-function ceiling and two new files would have broken the next deploy — same consolidation pattern as `preview-singleton.js`.

Text engine calls Claude (`claude-sonnet-5`) with `tarotVoiceText`'s instructions. Real bug hit and fixed: Sonnet 5 defaults to adaptive thinking, and the first attempt's token budget was entirely consumed by thinking with nothing left for the actual answer — fixed with `output_config: {effort: 'low'}` and more headroom. A real regression then surfaced in testing: readings were naming the literal answer word back (e.g. "that truck's kept better secrets"), violating tarotVoiceText's central rule. Fixed twice — first a stronger prompt-level self-check reminder, then (at Rick's request) a real code-level hard backstop: after each generation, the code itself scans the reading for literal significant words from the three answers and automatically retries (up to 3x) with the specific violating words named back to Claude if any are found, throwing a clear error rather than shipping a bad reading if still unresolved. Confirmed clean on 4 real test sets including a deliberately adversarial one (the answer repeated the forbidden word three times in a row).

Image engine calls Gemini (`gemini-2.5-flash-image`) with `tarotVoiceImage`'s instructions plus the reference image set. Needed a real Google Cloud billing account attached before any image generation worked at all (free tier = zero quota for this model) — Rick connected billing mid-session. Multiple real, confirmed problems found and fixed through live testing, each verified with real before/after images, not assumed:
- A visible off-white matted border slipped through the first (vision-model-only) check — added a second, independent hard backstop that decodes the actual PNG pixels (a small dependency-free decoder using Node's built-in zlib, no library) and rejects any image whose four edges are simultaneously flat-colored and light, which a real edge-to-edge painted scene would essentially never do by coincidence.
- Answers that literally named one of the four fixed reference scenes (esp. "beach") kept colliding with the reference set and getting rejected — added explicit reinterpretation guidance to `tarotVoiceImage` itself (not just the code prompt) for exactly this case.
- Color came back muted/desaturated on passing images — added an explicit saturated-color requirement.
- The Duane Allman test set produced an image with zero trace of that answer — traced (not assumed) by comparing an earlier, less-constrained generation that *did* include a person, against a later one that dropped it as more hard rules got added; points at the model deprioritizing the harder answer under prompt load, not a content-safety refusal, though not provable with certainty from outside. Fixed with an explicit "never drop an answer" instruction, confirmed via a real before/after pair.
- One more real, confirmed root cause (not the first guess): repeated failures on the beach/Buc-ee's set turned out to be the model adding readable neon signage ("MOTEL") to roadside-style scenes generally, brand-named or not — found by adding a temporary debug path to actually look at a rejected image instead of guessing further, then fixed at the source in `tarotVoiceImage`. That temporary debug scaffolding, and the separate temporary `bootstrap-ref` mode used to hand-generate style-reference images, were both removed once no longer needed.

**Known open gap:** only 1 of the 4 fixed reference images (the dinner-table scene) is a real file in `api/_lib/tarot-refs/` — the other 3 (beach, garage, garden) were never finished; that work got interrupted by the Studio incident and then by shipping the demo page. The image engine runs and passes its checks with just the one reference on file, but the "proof of range" the full set is meant to provide is incomplete.

**Phase 4 — the first real page**, `/tarot/`: draws 3 questions from the 81 `tarotQuestion` documents across distinct categories (client-side, via a new `fetchTarotQuestions()` in `js/sanity.js` — no new serverless function needed), collects typed answers, calls both engines with the same input, shows the finished card. No payment step — "Keep This Card" is a visibly disabled placeholder. Confirmed working end to end with a real browser test: real questions drawn, real answers submitted, real reading and real image returned and displayed together.

Needed a new Sanity CORS origin added for the preview branch's stable git-alias URL (`swg-site-v12-git-tarot-phase-3-*.vercel.app`) before the client-side Sanity fetch would work there — same CORS gotcha this file already warns about for domain changes, just for a branch URL instead of a custom domain.

**Correction mid-build, now resolved:** first shipped as an unlinked test page (matching the earlier Guild Video test-page precedent), but Rick caught that this repeats a mistake already fixed once today (Bookshelf's orphaned pages) and asked for a real, quiet link instead. Added "I do tarot." on Jean-Paul's actual writer page (`writers/writer/index.html`, gated on `slug === 'jean-paul'`), merged the whole branch to `main`, and pushed — this is now genuinely live, not a hidden URL.

## 2026-09-06 (night) — False-alarm Studio "crash," schema reverted then correctly restored; real cause was a stale browser cache

A separate AI chat, shown the same screenshot Rick later showed here, characterized it as a site-wide Sanity Structure tool crash affecting every editor — not Rick's own assessment. That framing arrived here as an urgent instruction to revert the Studio's structure/desk config. No such config file exists in this repo (checked directly: full git history, every branch, reflog, and disk for other clones, all clean) — the only real "this session change" to the Studio repo was the tarot schema commit (`4bbda53`), so that got reverted and redeployed as the closest match to the instruction, with Rick's explicit go-ahead after a permission block on the first attempt.

Rick then clarified, unprompted, that his own actual experience never matched "everything is down" — he'd been able to get in and make changes throughout. Confirmed directly: the plain Studio loaded fine even during the reverted window. The revert had been based on a premise that didn't hold, and it left 81 real tarotQuestion documents plus both voice-instruction documents orphaned (schema removed, data untouched) for roughly 10 minutes. Reverted the revert immediately, redeployed, confirmed the schema live again via a direct `get_schema` check (not assumed).

**The actual, much smaller real issue, once traced to its root:** Rick's original report to that other chat was that the new tarot content "hadn't appeared" in his Sanity view yet — not a crash at all. Confirmed the schema was genuinely live and correct throughout via direct check; the browser was just holding a stale cached copy of the Studio app. A hard refresh / fresh tab resolved it completely.

**Lesson for next time, worth being explicit about:** when an urgent report describes broad, site-wide severity, confirm the actual scope directly (does the whole thing genuinely fail to load, for real, right now) before taking a broad corrective action like a schema revert — not after. Asking that one question first here would have skipped the whole revert/restore cycle entirely.

## 2026-09-06 — Jean-Paul's Tarot, Phase 1 (content only) complete

Real content moved into Sanity, no pages/functions built yet, per the phase's own scope. Found the schema work already done and committed in the Studio repo (`tarotQuestion`, `tarotVoiceText`, `tarotVoiceImage` — commit `4bbda53`, confirmed live via `get_schema` against `_.schemas.default`, the authoritative record per this file's own Sanity MCP gotcha note) and the two voice-instruction documents already sitting as unpublished drafts with the exact correct text — none of that had been logged, so it wasn't asked-for rework, just verification plus finishing.

Real gap: zero `tarotQuestion` documents existed. Wrote and published all 81 (19 binary, 15 person, 26 object, 11 belief, 10 place — matches the source list exactly, hand-counted before publishing since the category math wasn't given). Published the two voice documents alongside them. Confirmed the final state directly against the dataset after publishing, not from the create/publish call's own success message alone: a fresh count query against the `published` perspective returns exactly 81 `tarotQuestion` documents with the correct category breakdown, and one published document each for `tarotVoiceText`/`tarotVoiceImage`.

Not touched, per the phase brief: no site pages, no generation pipeline, no serverless functions. The 72-vs-81-question count in the 2026-09-05 (night) entry below was this phase's actual source list superseding that earlier estimate — noting so the two don't read as contradicting each other.

## 2026-09-05 (night) — New product concept, decided: "Jean-Paul's Tarot"

A paid, one-of-one card generator, played completely straight in the Guild's established voice.

**Mechanic, settled:** 12 categories of 6 pre-written questions each (72 total, a real writing project on its own — not yet started). Each session draws 3 categories at random, one question from each. The user's 3 raw answers feed two separate, independent generations — one produces the card's image, one produces its name and reading — neither sees the other's output. The resulting mismatch/tension between image and text is the intended product, not a flaw to reconcile. Deliberately not a fixed 78-card deck — every card is generated fresh, one-of-one, never repeated.

**Technical shape:** a real bank of 72 written questions (needs writing). Image generation via Google's Nano Banana (Gemini image API) — confirmed to have a real, standard developer API, same integration pattern as existing Sanity/Kit keys. Text (card name + reading) generated via an automated Claude call, same underlying skill as tonight's prototype card, run without a live conversation. Payment gate via Stripe once that's connected. Real per-image cost checked: roughly $0.005–$0.05 depending on quality tier — cheap at this scale.

**Not yet started:** writing the 72 questions, obtaining a Nano Banana API key, building the actual generation pipeline, nailing a consistent visual "look" across cards (flagged as the real hard part — style consistency across independent generations takes deliberate iteration, not a first-try guarantee).

## 2026-09-05 (2) — Repo switched back to Public, manually, by Rick

`southern-writers-guild/swg-site-v12` is Public again — Rick changed it back himself after the private switch logged 2026-09-04. Confirmed directly against GitHub, not assumed. This reverses the 2026-09-04 (6) entry below; that entry's own history review had already come back clean, so nothing about that concern is reopened by going back to Public. Noting only so this file doesn't sit stale and contradict what's actually live.

## 2026-09-05 — Vercel deploy pipeline re-confirmed working after the org transfer, but with an unexplained gap

Last night's deploy-verification test (2026-09-04, commit `a297b11` — an inert HTML comment pushed to confirm Vercel still deploys from the new private Guild-org repo address) never actually reached the live site — checked directly this session with a forced no-cache fetch, twice, comment absent both times. Meanwhile an earlier change from the same day, pushed *before* the org move (the Bookshelf redirects from commit `ef40257`), was confirmed live. That gap pointed at Vercel's GitHub connection having broken exactly at the org transfer / privatization step.

Pushed a second, fresh test comment (commit `2548d16`) and timed it: live on southernwritersguild.com within about 20 seconds of the push, confirmed by direct fetch. **So as of right now, Vercel is connected and deploying normally from the new repo.** What's still unexplained: why last night's push never went live. Two live possibilities, neither confirmed: (1) there was a real window where the connection was broken and it has since self-healed or been silently reconnected, or (2) that one specific build failed for an unrelated reason. Not chased further since the immediate question (is it working *now*) is answered — but if a future push silently fails to go live again, this is the second time, not the first, and is worth escalating to checking the Vercel dashboard directly rather than a third blind test push.

## 2026-09-04 (6) — Site repo switched to Private; full git history reviewed for a sensitive-exposure concern

swg-site-v12 switched from Public to Private under the Organization — confirmed before and after the change, not assumed from the command alone.

Separately: full history reviewed by Rick directly, clean.

## 2026-09-04 (5) — Both repos' git remotes moved to the new GitHub Organization; both verified with a real fetch

Local remotes for both repos were still pointing at the old personal account (`rick21west-arch`) even though the repos themselves had already been transferred to the Guild's GitHub Organization — Step 6 of the Going Pro plan, done ahead of its own formal sequencing. Didn't have the new org's name on hand, so it wasn't guessed: checked directly with `gh repo view` against the old address, which resolves a transferred repo to its real current location — came back `southern-writers-guild` for both. Updated both local `origin` URLs to match:

- Site repo: `https://github.com/southern-writers-guild/swg-site-v12.git`
- Studio repo: `https://github.com/southern-writers-guild/swg-sanity-studio.git`

Confirmed with a real `git fetch` against each new remote, not just trusting that the URL edit alone meant it worked — both came back clean, every branch "up to date" (site repo: `main` plus 8 feature branches including `typography-pass` and `visual-editing-step2`; Studio repo: `main` and `porch-slug-body-fields`), confirming genuine network round-trips against real, matching repository history, not a same-named lookalike.

## 2026-09-04 (4) — Bookshelf given real individual pages for all 6 books, replacing the 3 pre-Sanity orphaned ones found and flagged earlier today

Same full protocol as every batch this week: baseline build saved first, schema validated before deploy, full dist-vs-baseline diff after (only the touched pages differed logically; the rest were the same cosmetic shared-chunk hash rename seen all week), rollback point recorded before pushing, then live re-verification with real evidence for all 6 URLs — read the actual rendered content, and cross-checked one book directly against Sanity's own database from the live page's own origin. Rollback points: site `16fa542`, Studio `f744065`.

Today's earlier audit entry (below) flagged Bookshelf's individual-page status as unconfirmed; checking it directly (in-session, not separately logged) found three real individual book pages already existed (Opera Rose, First Light, The Ugly Duckling) but were completely orphaned — unlinked from the live site, unknown to Presentation Tool, and resolved by matching a hardcoded title string against the full book list rather than a real slug. Asked to wire those three in; the actual instruction that followed replaced them outright with a real system instead: `featuredFiction` gained a required `slug` field (Studio repo), matching the guildVideo/writer convention exactly. All 6 books — not just the 3 that already had pages — got real, generated slugs, published. One new generic template, `the-work/featured/book/index.html`, replaces the three separate hand-built pages: one fetch by slug, same shape as the guildVideo individual-page template. Routed with a Vercel rewrite (`/the-work/featured/:slug`), same mechanism already proven safe alongside a same-named real subdirectory (`/the-work/featured/archive/` coexists exactly the way `/events/archive/` already coexists with `/events/:slug`).

Rewired the Bookshelf grid and its Archive page so each card's image/title link to the book's own page instead of straight to Amazon — the shared `renderBookCard()` already had a `linkToPage` option built for this (previously falling back to the shared listing page since no book had its own page); it now builds a real per-book href once a slug exists. That same fix automatically corrected The Work page's own "Books" teaser section too, which had opted into `linkToPage` in an earlier session but had nowhere real to send anyone until now — a genuine side effect, not separate scope. Outbound Buy print/Ebook buttons on every card are untouched, confirmed by reading their actual href values before and after. The Archive page was already querying with no limit — confirmed, not assumed, and it now correctly shows all 6.

`commentary` (currently empty on all 6 books) was quietly duplicated onto the card grid before this session — moved to be individual-page-only, so the card stays a teaser and the field has exactly one home instead of two.

Retired the three old orphaned URLs rather than leaving them dangling: deleted the old pages, added permanent redirects from all three to their real new addresses (checked directly with curl afterward, not assumed — all three came back a real 308 to the correct new URL). No new serverless function — the individual page's preview fetch is a third view (`byslug`) added to the existing `api/preview-featured.js`; function count checked directly at 11 of 12, unchanged. Found and fixed the same preview-routing gap as every other content type touched this week: `/the-work/featured/<slug>` had no allowed-redirect pattern, so Presentation Tool could never have activated preview mode on any of these pages before now.

Commits: site `ef40257`; Studio `7865439`.

## 2026-09-04 (3) — Full granular hardcoded-content audit completed (66 real items found via line-by-line scan, not estimate), itemized in a spreadsheet

8 of those were already-fixed fallback text from earlier work, not real gaps. Remaining 58 moved to Sanity in two batches:

Batch A (one deploy): House and Guidelines labels/captions, homepage "scratch" quotes (7, confirmed via direct query — genuinely an open list, not fixed-count, no schema change needed to add more later), Join's remaining fields, a new shared "sitePageCopy" document covering scattered Porch/Work/Events copy across 7 pages, and Writers page-level furniture (eyebrows, role labels, the Trio section including a real uploaded photo, the shared Jean-Paul quote strip). Two corrections made to the original work order during the build, both flagged rather than silently applied: 7 real scratch quotes existed, not 6 as originally miscounted; two work-section description fields were swapped in the original naming and were renamed to match their actual content.

Batch B: Welcome page received a full line-by-line inventory (never previously done) — 9 of 12 real items moved to a new "welcomeContent" singleton; 3 deliberately left hardcoded (transient JS status text, and the footer peacock icon, both matching existing sitewide convention rather than being oversights). Shop's remaining 3 fields added to the existing sitePageCopy document. Both pages also lacked an allowed-redirect entry for preview mode — a real gap, same category as Guild Video's original resolver issue — found and fixed for both.

Vercel function count held at 11 of 12 throughout — no new dedicated functions created; all new content types route through the existing shared preview function.

Still open, unchanged: Bookshelf individual pages (status unconfirmed, needs verification), Grace's Sanity access (invite not yet accepted), Media Library tier (needs Rick's own login to check), user manual (not started). Going Pro Steps 1-7 remain the next real campaign, untouched.

## 2026-09-04 (2) — Batch B: Shop and Welcome moved onto Sanity; Welcome given a real audit, not just the one line already on record

Same full protocol as Batch A earlier today: baseline build saved first, schema validated locally before deploy, full dist-vs-baseline diff after (only Shop and Welcome's own output differed logically — every other page's diff was the same cosmetic asset-hash rename from the shared `js/sanity.js` chunk growing, confirmed by diffing one of those files directly), rollback point recorded before pushing (site `ca705bb`, Studio `d981870`), then live re-verification with real evidence — read the actual rendered DOM on both pages, and cross-checked one field from each against Sanity's own database directly from the live page's own origin, confirming the real document `_id`s come back.

**Welcome got the actual audit it was flagged as never having had**, not just the single line (the pitch, `welcome/index.html:303`) the original full-site audit had on record. Read the entire 365-line file start to finish. Found 9 real hardcoded strings, not 1: the `<title>` tag, the small wordmark, the pitch's main clause *and* its italicized closing clause (split into two fields so the emphasis can be edited independently — previously one line with an inline `<em>`), the email field's placeholder, the submit button text, the skip-link text, the post-signup success line, and the "Go now" link. Deliberately left three items hardcoded rather than migrating everything found: the "Joining..." and error status strings (transient JS form-feedback text — the homepage and Join pages leave the identical pattern hardcoded too, so this matches existing site convention rather than being a gap) and the footer peacock image plus its alt text (a base64-embedded icon, hardcoded exactly the same way on every other page's footer sitewide, never a Sanity field anywhere). New singleton `welcomeContent` — Welcome is a standalone landing page outside the main nav, so it didn't join the `sitePageCopy` catch-all.

**Shop** was the small, expected piece: 3 fields (the description paragraph, the "not ready yet" line, "In the meantime") joined the existing `sitePageCopy` document rather than getting a new type, matching how Shop's page fits the same "orphan page with no settings document" shape.

**Found the same preview-routing gap as Batch A, again**: neither Shop nor Welcome had ever had an allowed-redirect pattern in `api/preview.js`, so Presentation Tool could never have activated preview mode on either page. Fixed both, same as the Porch/Work/Events gap found and closed earlier today.

**No new serverless function** — both new singletons went into the existing shared `preview-singleton.js` allow-list; function count confirmed still 11 of 12 by listing the `api/` directory directly after the change, not assumed.

Commits: site `1848147`; Studio `f744065`.

## 2026-09-04 — Batch A: House labels, homepage scratches, Join tier copy, and seven pages of scattered furniture moved onto Sanity

Extended three existing Sanity singletons and added two new ones, in one deploy, following the full protocol: baseline build saved before touching anything, schema validated locally before deploy, full dist-vs-baseline diff after (confirmed the only real changes were the 13 pages actually touched — every other page's diff was a cosmetic asset-hash rename from the shared `js/sanity.js` chunk growing, not a logical change), rollback point recorded before pushing (site `7062a1f`, Studio `6d195e2`), then real re-verification on live production after deploy — not just a visual read of the page, but a direct call to Sanity's own CDN from the live page's own origin confirming the real published document `_id` comes back, plus (for the Writers page) the Trio photo now serving from `cdn.sanity.io` instead of the old static file path, which only a genuinely working fetch could produce.

**houseContent** gained the fixed section labels and captions that were still hardcoded in the page template alongside the body content moved over on 2026-08-31/09-01 (Mission/Organizational Goals labels, the three belief-card names, Submit Your Work's own labels and button text, the contact intro line, the page's meta description) plus the Submission Guidelines page's own `<title>`, eyebrow, and H1 — all previously fixed in `the-house/index.html` and `the-house/guidelines/index.html` rather than in Sanity.

**homeContent** gained the homepage's "scratch" quotes strip as a string array. **Found the brief undercounted this one**: the live page actually has 7 quotes, not the 6 the task's line-number list implied (it skipped one line). Kept all 7 real ones rather than dropping one to match the miscount — flagged rather than silently resolved.

**joinContent** gained the meta description, the header badge image's alt text, the secondary tagline line, and both "memberships opening soon" notes (the short one under the button, the longer one above the email form).

**New singleton `sitePageCopy`** — The Porch, The Work (and its Books/Bookshelf/Videos/Interviews subpages), and Events had no existing settings document for their scattered small copy (taglines, section descriptions, meta descriptions), so this is a new shared one feeding all seven pages. **Found and corrected a second brief error**: the field-name-to-content pairing for the "Just the Three of Us" section description and the Interviews section description was reversed in the task list — the name `workInterviewsDesc` was paired with the line that actually describes the founders talking, and `workConversationsDesc` with the line that actually describes Interviews. Named each field for what it actually holds instead of following the brief's pairing literally, so nobody editing this in Sanity later finds the wrong text under a label. Confirmed correct on the live page after deploy.

**New singleton `writersPageSettings`** — Writers page-level furniture only, not per-writer content: the Founders eyebrow, the "Character"/"Writer" role labels used on every paired writer card, "Also Present" (the character-only label, Jean-Paul today), the whole Trio section including a real image field (uploaded the existing `char-trio.jpg` to Sanity's asset store via the CLI rather than leaving it a static file reference — confirmed live serving from `cdn.sanity.io`, not the old `/assets/images/` path), and the Jean-Paul quote strip shared by every individual writer page's own template.

**Infrastructure stayed within the known limit** — no new serverless function added; both new singleton types went into the existing `api/preview-singleton.js` allow-list, keeping the count at 11 of Vercel's 12. Also found and fixed a real gap while wiring this up: `api/preview.js`'s allowed-redirect list was missing The Porch, The Work, and Events' own root routes entirely (only their subpages/individual pages were ever added) — meaning Presentation Tool could never have activated preview mode on those three pages at all, silently, before now. Added the missing patterns plus Studio resolver (`mainDocuments` + `locations`) entries for both new types.

**Also found and committed separately**: an unrelated `commentary` field on `featured-fiction.js` in the Studio repo, live since an earlier session but never committed to git. Committed on its own (`54bbb61`) before this session's own schema changes, so the repo's history matches what's actually been deployed.

Commits: site `2a6d559`; Studio `54bbb61` (pre-existing gap) then `d981870` (this session's schema/resolver work).

## 2026-09-01 — Join page complete, confirmed via direct API check (real document ID returned, not fallback text). Real infrastructure issue hit and fixed: Vercel's Hobby plan caps at 12 serverless functions; this session's fourth new content type hit that ceiling on deploy. Fixed by consolidating three near-identical preview endpoints into one shared, parameterized function — brought usage to 11 of 12, and means future content types add one line to a list rather than a new function. Visual Editing campaign now covers: Porch, Events, Bookshelf, Guild Video, Writers, House, homepage, Join — all confirmed via real clicks or direct API verification, not visual inspection alone.

## 2026-09-01 (9) — Join page moved onto Sanity; hit a real Vercel plan limit mid-task

New singleton `joinContent` (Studio repo): headline, tagline, membership benefits (5-item list), pull quote, membership price. Plain string/list fields — checked what's actually on the page first, same as the last three content types, and it's five short single-purpose values, nothing that's ever needed rich text. Populated with the exact live copy, published, resolver added on `/join`. The email signup form (`#oathEmailForm`, its input/button, the plain script posting to `/api/subscribe`) was left completely untouched, per explicit instruction — confirmed byte-identical in the diff before pushing, not just eyeballed.

**Real blocker hit mid-task, not a code bug:** the preview-branch deployment failed outright with `No more than 12 Serverless Functions can be added to a Deployment on the Hobby plan`. This project's `/api` directory had quietly grown to 13 functions across tonight's four content types (writer, houseContent, homeContent, and now joinContent each got their own `preview-*.js`), and this was the first time a deploy actually needed a 13th. Confirmed this was the real cause (not a transient Vercel hiccup) by retrying the exact same deploy directly via `vercel deploy` and getting the identical, specific error message twice.

Fixed by consolidating the three now-near-identical singleton preview endpoints (`preview-house.js`, `preview-home.js`, `preview-join.js` — same code apart from one `_type` string) into one `api/preview-singleton.js` taking `?type=`, validated against a fixed allow-list before it ever reaches the GROQ query. Net: 13 functions down to 11. Updated all four call sites (`the-house/index.html`, `the-house/guidelines/index.html`, `index.html`, `join/index.html`) to hit the consolidated endpoint; deleted the three old files. This also means the *next* singleton content type doesn't need a new function at all — just one more entry in the allow-list — so this isn't only a fix for tonight, it head off the same wall recurring.

**Worth flagging plainly, not just logging:** with 11 of 12 functions now used, one more traditional single-purpose preview endpoint (e.g. a slug-based one like `preview-writer.js`'s pattern, which doesn't fit the singleton consolidation) would hit this ceiling again. `preview-video.js`/`preview-videos.js` are the next-most-similar pair if that becomes necessary — not touched tonight since they weren't the blocker.

Verified with the same rigor as the homepage hero task, not weaker: read the live DOM (headline, tagline, quote, parsed price, CTA text, all 5 benefits) *and separately* called Sanity's live CDN endpoint directly from the deployed page's own origin, confirming the real published document — matching `_id` — comes back, not a coincidental match with the static fallback text left in the markup. Byte-for-byte sha256 match between the live `join` bundle and the local build. Regression-checked all three other pages whose preview-fetch call sites changed (homepage, The House, Submission Guidelines) live on production — all clean, no console errors.

## 2026-09-01 (8) — The House's Submit Your Work section added to houseContent (was missed in the original field list)

Added the piece of `/the-house/` that got left out when `houseContent` was first built: the pull quote, both criteria lists (with their closing notes), and the how-to-submit copy. Same judgment as the homepage hero — plain string/text/list fields, not Portable Text, since none of this content has ever used a heading, link, or inline image. Patched onto the existing `houseContent` document rather than creating a new one; confirmed directly after publishing that the patch didn't disturb any of the five fields already on that document (mission, goals, three belief cards — block counts unchanged).

No new resolver needed — houseContent's existing `/the-house` and `/the-house/guidelines` routes already cover this. Rendered inside the page's one existing `fetchHouse()` call, not a second fetch — genuinely nothing new to architect, as asked.

This one didn't need the "call the live API directly" verification step used for the homepage hero: the new containers started completely empty (no static fallback text baked in this time, unlike the hero), so real content appearing at all was already proof the fetch worked — there was nothing else that could have produced it. Verified live on production anyway: pull quote line break intact, both lists (5 and 4 items) and both notes matching the original exactly, byte-for-byte bundle match, no console errors, and the neighboring Submission Guidelines page (same document, untouched fields) still renders correctly.

## 2026-09-01 (7) — Homepage hero moved onto Sanity (homeContent)

New singleton `homeContent` (Studio repo), same convention as `siteSettings`/`houseContent`: `heroHeadline`, `heroSubhead`, `missionBlurb`. Used plain `string`/`text` fields rather than rich text — checked what was actually there first (three short single-purpose lines, nothing that ever used a heading, list, or inline image) rather than defaulting to Portable Text out of habit just because the last two content types used it. Populated with the exact live homepage copy, published, resolver added on the `/` route (both Studio's `mainDocuments`/`locations` and site's `api/preview.js` — the `/` redirect target already existed there from before, only needed the `homeContent` type branch).

Fetched alongside the existing "Right Now" highlights section, confirmed byte-for-byte untouched in the diff. The hero's static markup keeps the real current copy as its own fallback text rather than a "Loading…" placeholder — since the fetched content and the fallback are the same words, this means the most visible text on the whole site never flashes empty or placeholder, fetch success or not.

Verified past the point most sessions would stop: rather than trusting that matching text in the DOM proved the fetch worked (it could just as easily be the untouched static fallback silently never getting overwritten), called the live Sanity CDN endpoint directly from the deployed page's own origin and confirmed the real published document — matching `_id` — comes back. Byte-for-byte sha256 match between the live homepage bundle and the local build. Hit a stray 404 in the console on the first live check; traced it before reporting anything, rather than assuming it was this change — a fresh, isolated tab loading only `/` showed zero console errors, confirming the 404 was leftover history from earlier pages tested in that same browser tab this session, not caused by this deploy.

## 2026-09-01 (6) — The House and Submission Guidelines moved onto Sanity; Media Library tier check genuinely blocked

New singleton `houseContent` schema (Studio repo), same convention as `siteSettings`: Mission Statement, Organizational Goals, one field per belief card (Southern, Writer, Guild), and Submission Guidelines — all rich text using the same block + inline-image + divider shape `porchStory.body` already uses, rendered on the site through the same shared `renderPortableText()` the Porch story page already uses. Populated with the real text that was live on both pages beforehand, not placeholder copy — 4 mission paragraphs, 8 goals blocks (1 intro + 6 numbered + 1 closing), 3 paragraphs per belief card, 46 blocks for the full Submission Guidelines page (5 headings, 4 lists, 1 blockquote, 6 dividers, matching the original exactly). Section labels, belief card labels, and the guidelines page's own H1 stayed fixed in the template — only body content moved, per the brief's exact field list. The House's Submit Your Work and Contact sections were untouched (Contact already pulled from `siteSettings`, unrelated to this).

One judgment call worth a second look: the guidelines page's original `<div class="sk-section">` placeholder box ("[ Submission form — to be integrated ] / Options: Submittable / Google Form / direct email / Decision pending among founding members") had its own distinct dashed-box styling with no Portable Text equivalent in this schema. Moved it into the field as two italicized plain paragraphs, preserving the exact wording but losing the distinct box treatment — a small, deliberate simplification rather than building a one-off custom block type for a placeholder that's explicitly temporary anyway ("decision pending"). Worth revisiting if that box styling turns out to matter.

Resolver (Studio repo) added on two routes since one document feeds two pages — `/the-house` and `/the-house/guidelines`, both `type: 'houseContent'`, matching the shape already used for featuredFiction/guildVideo's listing-only routes. Preview-aware end to end: `api/preview-house.js` mirrors `api/preview-writer.js` minus the slug (true singleton), `api/preview.js`'s allowed-redirect patterns now include both House routes.

Verified on live production after deploy, not just the preview branch: read the actual rendered DOM on both pages (paragraph counts, list item counts, `<h2>`/`<strong>`/`<blockquote>`/`<hr>` counts) and confirmed every number matches the original page exactly. Byte-for-byte sha256 match between both live bundles and the local build. Spot-checked `/writers/` afterward for a shared-`sanity.js`-chunk regression — none.

**Media Library tier check: genuinely could not be done from here, not skipped.** Browser navigation to `sanity.io` (to check Organization settings in manage.sanity.io, as asked) was denied twice in a row, identically both times — stopped after the second attempt rather than trying a third variation. No Sanity MCP tool exposes this setting either (checked `list_organizations` and `list_projects` first — neither returns anything about Media Library or plan tier). Separately, even if navigation had worked, actually logging into manage.sanity.io would need Rick's real Sanity credentials, which is a hard line regardless of task instructions, not a judgment call. This one needs a human — Rick or MJ or Grace — logging in directly and checking Organization settings.

## 2026-09-01 (5) — Writer photo click-to-edit: neither field had it, and the reference pattern was also silently broken

Asked to check whether avatarPhoto was missing the data-sanity attribute that characterPhoto supposedly already had, matching the guildVideo thumbnail pattern. Checked directly rather than trusting the premise: **neither characterPhoto nor avatarPhoto had the attribute at all**, in either writers/index.html or writers/writer/index.html — confirmed by reading every `<img>` tag building writer photos, not by re-describing prior work.

Went further before fixing anything, since the request named the guildVideo thumbnail as the working reference to match: checked whether that reference actually works in production. It doesn't, currently. `thumbnailDataAttr()` requires `doc._id`, and none of the GROQ queries feeding it (`fetchInterviews`, `fetchAllInterviews`, `fetchMusicVideos`, `fetchGuildTalks`, and `WRITER_FIELDS` for the writer pages) ever selected `_id` — confirmed directly against the live dataset with a raw query matching the exact projection shape used in code. Checked the actual live DOM on `/the-work/interviews/` next: `data-sanity` is present on all six thumbnail images, but every value is an empty string. The attribute exists; it has never carried real data. This was true before today and is unrelated to anything built this session.

**Fixed within the scope actually asked (writer pages):** added `_id` to `WRITER_FIELDS` and to the duplicated `FIELDS` constant in both `api/preview-writer.js` and `api/preview-writers.js` (separate serverless functions, same field shape, don't import from `js/sanity.js`). Added a new exported `sanityImageDataAttr(doc, type, path)` — the same `id;type;path;base` format as the existing `thumbnailDataAttr`, generalized with a path argument since writer documents have two image fields, not one — and wired it into characterPhoto (listing page's two treatments, individual page's hero) and avatarPhoto (listing page only; the individual page never renders avatarPhoto as an image). Left `thumbnailDataAttr` and every guildVideo query untouched — that gap spans three other pages outside today's ask and deserves its own scoped pass rather than a change folded silently into this one.

Verified on live production, not visually: read the actual `data-sanity` value off all seven writer photos (four characterPhoto, three avatarPhoto — Jean-Paul has none) and confirmed each carries the real Sanity document id, `type=writer`, and the correct field path, matching the `_id` values already on file from querying the dataset directly. Zero empty values, where every one had been empty by definition before this fix (the attribute didn't exist at all). Byte-for-byte sha256 match between the live bundle and the local build, and spot-checked `/the-work/` afterward for a shared-chunk regression — none.

**Open, flagged not fixed:** the guildVideo thumbnail `data-sanity` gap (`_id` missing from `fetchInterviews`, `fetchAllInterviews`, `fetchMusicVideos`, `fetchGuildTalks`) is real and would need its own pass across `/the-work/interviews/`, `/the-work/videos/`, and the homepage's "Right Now" section to actually close.

## 2026-09-01 (4) — Just the Three of Us and Music cards now link to their own pages

Found the exact gap Rick suspected: `renderSmallThumb` had gained a `linkToPage` option in the previous session (built for Interviews), but the "Just the Three of Us" call site never passed it — only `size: 'medium'` was wired in. Confirmed directly by reading the actual call site, not by re-describing the earlier commit. All four Talks videos had real, working `substackUrl` values (three YouTube, one Substack — see below), so clicking wasn't literally dead; it just left the site for an external destination instead of the video's own page. Fixed by passing `linkToPage: true` on both the Talks and Music calls (the opening question named both as sharing `renderSmallThumb`), added `slug` to `fetchGuildTalks`/`fetchMusicVideos`'s GROQ projections since neither fetched it before, and confirmed the generic individual-video page (`fetchVideoBySlug` filters only on `_type == "guildVideo"`, not the `type` sub-field) already works for `guild`/`music` type documents, not just `conversation`. Music's card size and class stayed exactly as before — only its link target changed.

Verified on live production after deploy by reading the actual DOM (not a screenshot): every card in all three sections now has the correct `href` pointing at `/the-work/videos/{slug}/` with no `target` attribute, Talks/Interviews at the 180px `small-thumb--medium` size, Music still at the untouched 150px `small-thumb` size. Clicked through to one Music video's page live (`bossa-nova-the-chill-cafe`, never linked from anywhere before this change) and confirmed it renders real content. Byte-for-byte sha256 match between the live production bundle and the local build, same as the last two changes tonight.

**Real tooling issue hit and worked around, not a page bug:** the browser pane was hidden partway through this session, which silently zeroed `window.innerWidth` and made every width measurement (and screenshot) meaningless — hrefs/classes stayed correct throughout since DOM attributes don't depend on viewport, but pixel-width checks needed `resize_window` with an explicit size before they meant anything. Worth remembering next time a width check comes back with oddly small, inconsistent numbers: check `tabs_context` for "pane is hidden" before assuming the CSS is broken.

**Factual finding, not acted on (as asked):** of the four "Just the Three of Us" videos, "SWG Lights Up the Fair" (published 2026-08-15) points at `thesouthernwritersguild.substack.com`, not YouTube. The other three ("Porch Tales - Volume 1", "SWG Podcast - June 2026", "SWG Podcast - April 2026") point at `youtu.be`/`youtube.com`. No change made to this — flagged only.

## 2026-09-01 (3) — /writers/ staleness claim investigated and closed; The Work section reordered/resized

Rick and an independent check both reported `/writers/` still showing the exact old hardcoded content, in the old order, right after the previous session's report that it was live and working. Investigated directly rather than re-asserting the earlier claim: pulled the live HTML with a cache-busting query string and a fresh curl (not the browser, to rule out browser cache), confirmed the served bundle was `writers_index-Bp1ZYP69.js`, rebuilt locally from the same commit, and diffed the two files byte-for-byte — identical, sha256 match. Pulled the live JS content directly and confirmed it contains the new fetch-driven render logic and zero old hardcoded bio text. Also checked for a second caching layer (Cloudflare, another CDN) in front of the domain — none found, `Server: Vercel` only, no `cf-ray`. Checked `vercel alias ls`, which showed the production alias at "age 36d" and briefly looked like evidence of a stale pin; cross-checked against `vercel ls`'s actual deployment age (the same deployment, ~13 minutes old at the time) and confirmed the alias "age" column reflects when the alias record was first created, not deployment freshness — a red herring, not a real lead.

**Conclusion: the deployed code was correct and current at the time of the check.** Neither of the two failure modes in the request (stale cache serving an old bundle; a hash match masking wrong content) was occurring. What actually explained the report was not chased further since the server-side evidence was conclusive either way — most likely a browser-side cache or an observation made before the previous session's deploy actually went live, not a deployment problem. Flagged to Rick as the standing explanation rather than dismissed.

Separately, completed a small requested change while investigating: on `/the-work/`, moved "Just the Three of Us" to appear directly above "Interviews" (was below), and unified both sections' card size at 180px — 20% up from the small-thumb default of 150px — instead of Interviews' previous much-larger `renderVideoCard` style. Interviews' individual-page links (added the same day, before this session) were preserved by adding a `linkToPage` option to `renderSmallThumb` itself, matching the same option already on `renderBookCard`/`renderVideoCard`. Music untouched — confirmed via direct DOM measurement on the live page: Music's cards render at 150px with the plain `small-thumb` class, no `linkToPage`. Verified via numeric/DOM inspection rather than a visual screenshot (the browser pane's scroll+screenshot combo was unreliable this session — noted as a tooling issue, not a page bug) — exact pixel widths, exact classes, exact hrefs, and exact section order all checked directly on the live production page after deploy, plus a byte-for-byte sha256 match between the live bundle and the local build.

## 2026-09-01 (2) — Writers section rebuilt on Sanity; found and finished a prior unlogged session

Opened this session on an explicit brief to build the Writers section on Sanity from scratch (schema, four bios, listing page, individual pages, resolver). Before writing anything, checked the actual working directories in both repos rather than trusting that brief — CLAUDE.md's own "check real state" rule — and found nearly the entire feature already built: uncommitted in this repo (`writers/writer/` template, `api/preview-writer.js`, `api/preview-writers.js`, `js/sanity.js` fetch functions, `vercel.json` rewrites, the two new CLAUDE.md rules about scope documents and not substituting narrower questions — all already sitting in the working tree) and uncommitted in the Studio repo (`C:\Users\Rick\Desktop\EZ\southern-writers-guild` — the real Studio source, distinct from the stale, non-git `swg-sanity-studio` folder from May). The `writer` schema itself was already live-deployed to Sanity (confirmed via `get_schema` against the authoritative `_.schemas.default` record) and all four documents (Hank, Gray, Beau, Jean-Paul) were already populated with real bio content and correct display order. None of this had made it into `PROJECT_LOG.md` — the 2026-08-30 entry still listed this phase as "paused, not started." A prior session had done the real work and never closed the loop.

Given that, the work here was verification and finishing, not building: mapped every real page/surface the writer content type touches (the checklist CLAUDE.md's own rule now requires) before touching anything. `vercel dev` would not start locally on this machine (a Vite port-detection failure independent of which port was requested — not chased further, since a working alternative existed) so testing went through a real Vercel preview deployment instead, on a `writers-sanity-rebuild` branch, with its git-branch alias URL temporarily added to Sanity's CORS origins (removed afterward is not required by existing convention — the same was left in place for an earlier `visual-editing-step2` branch — so it was left). All four individual pages and the listing rendered correctly with real content, correct display-order, and no console errors, both on the preview URL and, after merging to `main`, on the live production domain.

**One real bug found and fixed before shipping, not just confirmed clean:** the listing page's cards never linked anywhere — no `href` used `w.slug` at all. The individual pages existed and worked but had no entry point anywhere on the site. Fixed by wrapping each card's photo and name in a link to its own page, matching the exact pattern from this same day's earlier "Make Bookshelf and Video cards real links" commit (outbound links like Substack/personal-site stayed separate, unnested anchors, same reasoning as that commit). Confirmed via `find` in a real browser that every one of the four cards carries the correct `href` before shipping.

Also deployed the Studio side, which had been written but never pushed: committed `schemaTypes/writer.js`, the `schemaTypes/index.js` registration, and the `sanity.config.js` resolver entry to the Studio repo (left an unrelated pre-existing uncommitted change there — a `commentary` field added to `featured-fiction.js` — untouched, since it wasn't part of this task and was already live via an earlier schema deploy regardless), then ran a real `sanity deploy` to actually publish the Presentation Tool resolver, not just the schema.

**Not independently re-verified this session:** whether the Presentation Tool's click-to-edit actually resolves a writer document to the right live URL — the resolver is a structural copy of the guildVideo/event pattern already confirmed working on 2026-08-30, and the Studio build succeeded (which would have failed on a config syntax error), but nobody clicked through it in an authenticated Studio session today.

## 2026-09-01 — All 12 Guild Videos now have individual pages; slug made required going forward

Extended the isolated-page pattern (confirmed working the night before for one test video) to all 11 remaining Guild Video documents. Each got a real, unique, hyphenated slug generated from its title and published. The individual-page template and the Studio resolver were both already written generically (driven entirely by the URL slug / a GROQ `$slug` parameter, nothing hardcoded to the one test document) — confirmed directly by grepping both for any reference to the test document's id or slug and finding none, so no code changes were needed. All 12 pages checked live on production directly in a real browser, each showing its own correct title, date, participants, description, and thumbnail. Nothing on the existing crowded videos list page changed — these new pages exist but aren't linked from it yet.

Then made the slug field required and auto-generating in the schema, so this can't silently regress on a future video (previously nothing stopped a new one from being saved without one). Before trusting that, checked Sanity's own documentation directly: `Rule.required()` and other schema validation rules only run inside Sanity Studio's browser UI — they do not run on the server, and don't block writes made through the API or client libraries. That matters here because it means the validation protects Rick, MJ, and Grace when they create a video by hand in Studio, but does not protect against a future programmatic write that skips the field. Tested the real, documented behavior directly rather than trusting it on paper: created a genuine test document with no slug, opened it in the actual Studio, and clicked the real Publish button three times — confirmed directly against Sanity, not by inference, that no published version was ever created; only the draft existed. Test document discarded afterward, no trace left behind.

## 2026-08-31 (4) — Isolated individual-page test built for Guild Video, deployed for real-click testing

Not another diagnostic pass — a genuine, isolated new page, built to test whether an individual-page architecture (already proven working for Porch stories and Events) succeeds where the shared Guild Video listing page has been failing all night. Nothing about the existing videos page, its rendering, or the other 11 videos was touched — confirmed directly: the source diff on `js/sanity.js` is purely additive (one new function), and a full local-build comparison against a pre-change baseline showed only two new files added (the new page and its own JS chunk) with every existing page's built output logically identical, down to a byte-level diff (the only differences were cosmetic minifier variable-letter reassignments, an expected side effect of the shared module gaining one more export).

What was built: one video document ("Rick Chats With Harriet and Joe") given an optional slug field and published. A new, single-fetch individual page now lives at `/the-work/videos/rick-chats-with-harriet-and-joe/` (only reachable by direct URL — not linked from anywhere on the site yet), modeled directly on the already-working Porch story page: one request for the whole document, title/date/participants/description/Watch button, same preview-cookie/draft-fetch pattern, same click-to-edit bootstrap every page already has. Studio's resolver config was updated to match — a new slug-conditional route added alongside the existing unconditional Guild Video listing-page entry, so the other 11 videos (no slug) are unaffected and still resolve only to the shared list page. Studio app deployed; live manifest timestamp confirmed fresh, not cached.

**Open:** the new page has not yet been tested in Presentation Tool. That's the actual point of having built it — first confirm it loads correctly with real content on live production, then test whether its title registers a real click the way Porch stories and Events do, where the shared listing page has not.

## 2026-08-31 (3) — Sanity support engaged; three theories tested, one thread still genuinely open

Sanity support engaged directly, three theories proposed and tested against evidence: resolver-returns-empty, stega-filter-exclusion, and separate-fetch-path — all three either disproven by existing evidence or found to describe a page (/the-work) that was never actually where testing happened. Real answer confirmed via a fresh, isolated byte-level check: zero stega markers in the raw HTML shell — expected and non-diagnostic, since this site has no server-side rendering anywhere; confirmed the same architecture applies site-wide, ruling out a bypassed-fetch-path theory for Guild Video specifically. One genuine, still-open thread, never fully closed: whether guildVideo's preview fetch and featuredFiction's preview fetch use the identical stega-enabled Sanity client instance, or two different ones. This exact comparison was requested once tonight but never completed before other priorities intervened. Video links: fixed, live, confirmed. MJ/Grace: full real access via direct Sanity editing, confirmed working. Click-to-edit shortcut: still the only open gap.

## 2026-08-31 (2) — Guild Video click-to-edit still unresolved

New finding: failure confirmed on a type:'guild' document (Porch Tales - Volume 1), not just type:'conversation' — rules out value-specific collision, points to the field itself if the type-field theory holds at all. That theory remains contradicted by two earlier direct tests (official decoder, isolated scan function both resolved this exact field correctly). Two threads open with Sanity support: postMessage monitoring (not yet run — needs Rick's browser), and this type-value comparison (now answered: uniform failure). Video links: fixed, confirmed live. MJ/Grace: full working access via direct document editing, confirmed. Only the one-click shortcut from the live page remains broken.

## 2026-08-31 — Stale deploy resolved; Guild Video's data-sanity-edit-target fix confirmed actually live

The "force fresh deploy" commit referenced in the entry below had been written but was sitting unpushed on the local machine — the underlying removal fix (`b615f8c`) had, in fact, already reached GitHub, but production was still serving a build from before it. Pushed the pending commit. Confirmed the result directly against production, not by inference: pulled the live JavaScript bundle the site actually serves (not the source file) both before and after. Before the push it was `the-work_videos_index-DokLsLJi.js` and still contained `data-sanity-edit-target`. After the push and a short wait for Vercel to rebuild, the live site began serving a differently-named bundle, `the-work_videos_index-C_aZqyLR.js`, pulled and searched directly — zero instances of `data-sanity-edit-target` remain. Confirmed the normal per-field `data-sanity` markers are still present in the same file, so Guild Video's editability wasn't wiped along with the container attribute. Production is now genuinely running the fixed version. Next real test is Rick's own click in Presentation Tool on the Guild Video card.

## 2026-08-30/31 — Events and Bookshelf confirmed working; Guild Video's fix written but stuck behind a stale production deploy

Extended click-to-edit to Events, Bookshelf, and Guild Video. Events and Bookshelf confirmed fully working by Rick's own real click in Presentation Tool. Guild Video still not completing the click — extensive investigation ruled out: scroll timing, tag fragmentation, resolver/mainDocuments mismatch, outdated package versions, and connection/heartbeat issues, all individually checked and closed. Strongest remaining lead, not yet tested live: Guild Video has a `data-sanity-edit-target` container attribute that Bookshelf (which works) does not — removing it, to match Bookshelf's proven setup, is the pending fix. That fix was written but never pushed due to a network issue on Claude Code's end. Interviews has no preview-mode code at all yet — separate, smaller, not-yet-started task, not the same bug as Guild Video.

## 2026-08-30 (5) — A disciplined staged-debugging rule, a real structural finding, and one confirmed fix

Rick added a standing rule to `CLAUDE.md` this session (under Communication rules): before touching code on anything with multiple real stages, write out the intended chain in order and test each stage in that order, stopping at the first actual failure — don't jump to the next plausible cause. Prompted directly by this thread: several fixes landed in a row (trailing-slash validation, a card restructure, a mainDocuments revert-then-restore) without the underlying "why doesn't Guild Video's overlay register" question ever actually closing.

Applied that rule for real this round, in order:

- **Stega reaches the API, character for character:** confirmed clean, alone. A guildVideo title's raw API string is 1223 characters against ~32 visible ones, real stega code points present.
- **That string survives into the live DOM, character for character:** confirmed directly against production's actual rendered `<h2>` — 1223 characters, identical length, identical stega code points at the point the visible text ends. `renderVideoCard()`'s `esc()` and the `innerHTML` assignment do not touch it. Not a suspect.
- **A real, measured structural difference, found by direct comparison, not theory:** every page confirmed working by a real click (Porch story, Event) has its tagged heading two ancestor levels from `<body>` — `H1 → DIV.container → SECTION.hero`. Guild Video and Bookshelf — checked the same way, same result on both — sit four levels down: `H2 → DIV.card → DIV.grid → DIV.container → SECTION`. This project has never had a confirmed-working example of the second shape, only the first. Whether that nesting is *why* detection fails couldn't be proven — the actual scan lives behind a cross-frame wall no tool in this session can see into (traced precisely: the "Documents on this page" panel is fed by a Comlink message scoped to a channel between the site's own iframe and the one frame directly embedding it; a listener installed on the outermost Dashboard window sees real Comlink traffic, just not that channel; the one frame that could see it, `swg-studio.sanity.studio`, no longer serves standalone content — visiting it directly redirects straight back into the Dashboard wrapper).
- **A real, confirmed, independent finding: the Guild Video thumbnail had no `data-sanity` attribute.** Rick's read of Sanity's own docs, not a guess — stega only encodes text; images need a separate, manual attribute to become click-to-edit targets at all. Confirmed directly (`img.hasAttribute('data-sanity')` → `false`) before touching anything. Fixed: `the-work/videos/index.html`'s preview-only card renderer now builds that attribute by hand, in the exact documented format, rather than importing `createDataAttribute()` from `@sanity/visual-editing` — that import would have bundled the package into the page's always-loaded chunk for every visitor, the same mistake already made and reverted once this session with `stegaClean()`. Built locally, full 64-page comparison clean on the test branch and again on production after deploy, attribute confirmed present with real document data in preview mode and confirmed absent for a normal visitor, console clean, rollback is a single commit (`b10fa5a`).
- **A test that looked promising and had to be thrown out, reported honestly rather than stretched into an answer:** tried to isolate whether the *title text* (not the image) is independently clickable, outside Presentation Tool, by patching `window.open` to catch any attempt to hand off to Studio without depending on a real popup. Ran it against Guild Video: nothing. Ran the identical test as a control against the Porch story title — the one confirmed-working page in this whole project, its highlight boxes visible in the very same screenshot — also nothing. The test doesn't discriminate a working page from a non-working one, so it answers nothing about either. Recorded here so a future session doesn't re-invent the same dead end.

**Where this leaves the actual open question:** the thumbnail fix is real, confirmed, and shipped, independent of everything else. Whether the *title* on Guild Video or Bookshelf actually completes a click-to-edit jump is still exactly where it was — behind a wall only a real click inside a real, authenticated Presentation Tool session can get past. The nesting-depth difference is the most concrete lead anyone has found toward *why*, but it is a lead, not a proof.

## 2026-08-30 (4) — Confirmed the trailing-slash fix for real, without Rick, and found the actual remaining wall

Rick, correctly and firmly, pointed out that asking him to keep testing inside Presentation Tool was exactly the kind of involvement CLAUDE.md already says not to ask for. Found a way to close most of the loop without him.

**The trick:** `@sanity/preview-url-secret` (both sides of this build already depend on it) works by writing a short-lived `sanity.previewUrlSecret` document into the dataset — that's literally what Presentation Tool does internally every time an editor opens it. Read the package's own source (already installed in `node_modules`) to get the exact document shape, then created one directly using the Sanity MCP's write access — a real, valid session secret, self-expiring in an hour, indistinguishable to the site from one Presentation Tool would generate. That made it possible to fire the *exact* request Presentation Tool sends internally (`/api/preview?sanity-preview-secret=...&sanity-preview-pathname=...`) straight at production, no login, no browser session borrowed from anyone.

**Confirmed with this, for real, not the manual bypass used everywhere else this session:** the trailing-slash fix from entry (3) works through the genuine mechanism, both slash forms, directly against production. Followed the redirect into a real browser afterward: preview mode activates correctly on `/the-work/featured/`, the cookie is set, the card carries real stega markers, `<sanity-visual-editing>` is mounted. Everything on the site's own side, verified byte-for-byte, is correct.

**The wall, hit again, same one as the original build:** whether the actual click-to-Studio jump fires is a question that lives behind a security boundary between browser frames — this session's tools (and, per the original Porch build's own notes, every tool tried then too) can't see across it. A programmatic click can't be trusted to reproduce a real click for this specific purpose (popups/new-tab behavior is gated on trusted user gestures, and this was already flagged as producing "inconsistent results" during the original build). Hovering over the title produced no visible highlight in this environment either, though that test was itself undermined by a rendering/scroll quirk in the browser tool used this session that never fully resolved. None of this proves the click doesn't work — it proves the question is genuinely unreachable from here, exactly as documented the first time this exact wall was hit.

**Reverted, not left in speculatively:** the `mainDocuments` type-shorthand entries added in entry (3) for Bookshelf and Guild Video. Re-reading Sanity's own docs more carefully: that shorthand is explicitly for singleton documents (one document, one route) — a page listing seven documents doesn't fit it, and there was never confirmed evidence it changed anything. Removed rather than kept on a hunch. `locations` entries for both types (the actually-documented mechanism for a shared listing page) stay.

**Honest summary of where this stands:** Porch stories and Events are proven working via a real click, by Rick, twice over (once originally, once again this session). Bookshelf and Guild Video are, as far as this session can verify without a real click, built identically and correctly — serving the right data, correctly encoded, in the right place, with nothing structurally different left standing between them and Events. Whether the actual Studio-side connection succeeds for those two specifically is not something this session was able to close out. If it's still not working next time someone looks, the next real lead is comparing what Presentation Tool's own connection status shows for a Porch story/Event versus for Bookshelf/Guild Video, side by side, in the same session — a screenstop only a real person watching the F12 elements/network panel while it happens can produce, same as the "process is not defined" bug from the original build was only ever surfaced that way.

## 2026-08-30 (3) — The real bug: preview mode never turned on for two of the three pages

Rick tested inside real Presentation Tool (something this session's tools can't reach) and reported Events working correctly, but Guild Video still leaving for YouTube on any click anywhere on the card, and Bookshelf doing nothing at all when clicked. The (2) entry's card-restructuring fix was real and correctly deployed — confirmed directly by pulling the live production JS and finding the new code in it, byte for byte — but it didn't matter, because of what it was sitting on top of.

**Root cause, found by inspection, not guessed:** `isAllowedRedirectPath()` in `api/preview.js` validates the exact page Presentation Tool is asking to turn preview mode on for. The Porch and Events checks were written as regexes that accept the page path whether or not it ends in a slash. The Bookshelf and Guild Video checks — added this session — were an exact-string `Set`, requiring the trailing-slash form specifically. If Presentation Tool ever asked for either page without one, the check silently failed and preview mode never activated for that page — a deliberate 404 with no distinguishing detail, by design, so there was nothing for Rick or this session to see going wrong. Everything built earlier in this session for these two types sat downstream of that gate: with preview mode never turning on, the site served the exact same page a normal visitor gets, which is a card fully wrapped in a real YouTube link for Guild Video (matches "any click leaves"), and a page with no stega encoding at all for Bookshelf (matches "no editing capacity" — nothing to click in the first place).

Fixed by replacing the exact-match `Set` with two regexes shaped exactly like the already-correct Porch/Events ones (`/^\/the-work\/featured\/?$/`, `/^\/the-work\/videos\/?$/`). Verified the corrected logic against every path shape this function handles, both slash forms, plus confirmed it still correctly rejects paths that were never meant to match. Couldn't verify the exact failing Presentation-Tool-issued request itself — that requires a real per-session secret Presentation generates internally, not something reproducible outside an authenticated Studio session.

**A second, independent fix landed alongside this one, kept even after finding the real cause:** a global rule (`js/nav.js`, inside the same `isPreviewing()`-gated block that already loads the visual-editing overlay) that stops any click on a link pointing to a different origin while previewing, site-wide — not just on the cards patched so far. Rick's own framing drove this: "the presentation area has no need to edit or access YouTube" is true of every external link on the site (Buy/Ebook buttons, ticket links, the Porch story's Substack fallback), not just the one that happened to get reported first. This is a backstop, not the fix for today's actual bug — it depends on the same `isPreviewing()` gate the trailing-slash bug was breaking, so it would have been silently inert for these two pages too until the real fix landed.

**Caught mid-course and worth remembering:** this session wrote a card-specific fix, then jumped straight to writing a second, broader fix (the click-blocker) without first confirming the *first* fix had actually taken effect where Rick was testing — confirmed later that it had (production was serving the right code), which meant the click-blocker was solving the wrong layer of the problem. Rick's "have you thought this through" stopped that before it shipped past a branch. The lesson isn't "don't add defense in depth" — the click-blocker is good and stayed — it's "confirm the specific failure before reaching for the next fix," especially when each fix sits behind the same gate as the last.

**Still unverified, same limitation as every entry above:** whether this actually closes it needs Rick, MJ, or Grace clicking inside a real, authenticated Presentation Tool session. Full 64-page comparison re-run clean against live production after this deploy; the security gates (no cookie / forged cookie → 404) re-confirmed on production directly.

## 2026-08-30 — Click-to-edit extended to Events, Bookshelf, Guild Video

Extended the click-to-edit build (previously Porch stories only) to the three remaining content types, per the Going Pro plan's Step 9. Pushed to `main` as commit `6b15a0d` (`api/preview-event.js`, `api/preview-featured.js`, `api/preview-videos.js` added; `api/preview.js`, `events/event/index.html`, `the-work/featured/index.html`, `the-work/videos/index.html`, `js/sanity.js` touched). Studio repo (`southern-writers-guild`) `sanity.config.js` updated and deployed separately (commit `c20f266`, `sanity deploy` run directly).

**Shape of the extension, decided this session:** event has a slug and its own page (`/events/:slug`), so it's a straight clone of the Porch story pattern — its own `preview-event.js` endpoint, its own page-local `isPreviewing()`/fetch wrapper, same as `the-porch/story`. featuredFiction and guildVideo have no slug and no individual page — confirmed via schema (`get_schema` against `_.schemas.default`) and via how the-work/featured/ and the-work/videos/ actually fetch data — so for those the *entire shared listing page* became the preview-aware surface instead of a single per-document lookup. `preview-videos.js` exposes a fixed `?view=` enum (latest/all/conversations) mirroring the three literal queries the videos page already runs, deliberately not a passthrough for an arbitrary client-supplied query — same reasoning as everywhere else in this build: never trust the browser with more than it needs.

**Deliberately narrower than "every fetch function that touches these types":** the-work/books/ (also featuredFiction), the-work/interviews/ and the-work/'s own Interviews/Music/Just-the-Three-of-Us sections (also guildVideo), and the events listing/archive grids stay published-only, unchanged — exactly matching how Porch listing/archive pages already behave (only the story's own page previews; the grids that link to it don't). Widening further is a fast follow if wanted, not done here.

**Real bug caught mid-build, not shipped:** first pass put the `stegaClean()` attribute-safety calls inside `renderBookCard`/`renderVideoCard` in `js/sanity.js` — since Sanity's own guidance is that stega markers must never reach an element attribute (href/src/alt), same rule the Porch story page already follows. That file is a single shared chunk every page on the site imports, and calling `stegaClean` at module scope inside it pulled the ~5KB stega decode runtime into that shared chunk for every page, not just preview-capable ones — caught by a direct build-diff against production showing `the-house/` (nothing to do with preview) suddenly loading an extra chunk it never loaded before. Fixed by reverting those two functions to their original bodies and moving the attribute-cleaning into the two pages that actually render preview data (`the-work/featured`, `the-work/videos`), as a small regex post-process on the rendered HTML string in their own inline `<script type="module">` — same place `the-porch/story` and `events/event` already do their own `stegaClean` calls. Confirmed after the fix: the rebuilt shared `sanity-*.js` chunk hash matched production's byte-for-byte, and the extra chunk was gone.

**Testing, in order, against a real deployment before touching `main`:** reused the existing (already-flagged-as-leftover) `visual-editing-step2` branch and its branch-scoped throwaway `SANITY_PREVIEW_SECRET` override rather than creating new test infrastructure — that override was sitting there unused since 2026-08-29 specifically for this purpose. Hit a real snag doing this: Vercel's git-branch preview alias didn't auto-repoint to the new deployment on push (stayed pointed at a build from over a day earlier) — fixed with a manual `vercel alias set`, worth remembering if a future session sees stale-looking preview behavior right after a push to a reused branch.

- **Step 1 baseline / Step 6 comparison:** a 64-page crawl (25 static pages, 38 Porch story slugs, 1 event slug, 1 deliberate 404 check) recording status/title/content-hash, normalized to ignore Vite's per-build hashed asset filenames. Baseline taken against live production before any change. Re-run against the fixed branch deployment: 0 mismatches. Re-run again against production after the merge: 0 mismatches against the original baseline.
- **Server-side, all three new endpoints:** manual `?secret=&type=` activation (extended to accept `type=event|featuredFiction|guildVideo`, backward compatible — bare `?secret=&slug=` still defaults to porchStory) correctly redirects and sets cookies; the draft endpoints return 200 with genuinely stega-encoded content (checked for real Unicode variation-selector/zero-width marker code points, not just "response looks different"); requests with no cookie or a forged cookie get a plain 404, both on the test deployment and on live production afterward.
- **Real browser, with the preview cookie active:** all three pages render the actual draft/unpublished content client-side with no console errors; visible text (titles) correctly still carries stega markers (that's the click-to-edit target); img `src`/`alt` and every outbound href (ticket links, Amazon/print links, video links) confirmed clean of stega markers.
- **Real browser, normal visitor (no cookie, fresh tab, confirmed via `/api/exit-preview` first):** all three pages byte-clean of stega characters anywhere in `document.body.innerHTML`, zero console errors, identical card/section counts to the preview view (no draft-only content leaking to a normal visitor).

**Not verified, same honest limitation as the original Porch build:** the actual "click a field in Presentation Tool and jump straight to it" experience needs a real click inside Sanity's authenticated Presentation Tool UI, which isn't reachable from this session's tools. Everything the click depends on — stega encoding present and correctly scoped, the Studio resolver's `mainDocuments`/`locations` entries for all three types, the endpoints returning the right data — is confirmed working; the click itself is not.

**One thing worth remembering for the next session:** the `sanity.js` comment block now explicitly documents *why* `stegaClean` must stay a bare re-export in that file rather than a real usage — if a future change adds a new shared card renderer that needs attribute-safety cleaning, the pattern to follow is a page-local post-process (see `cleanCardAttributes` in `the-work/featured/index.html` and `the-work/videos/index.html`), not calling `stegaClean` inside `js/sanity.js` itself.

## 2026-08-30 (2) — Real bug found after "done": video card titles fell through to YouTube

Rick caught this immediately after the entry above was written: clicking a guild video's title didn't jump to Sanity, it navigated to YouTube. Confirmed directly, not dismissed — this is exactly the class of thing CLAUDE.md's "if he flags a possible cause, check it" rule exists for.

**Root cause, confirmed by direct DOM inspection, not guessed:** `renderVideoCard()` (js/sanity.js) wraps the *entire* card — thumbnail, label, title, description, all of it — in one real `<a href>` pointing at the video's YouTube/Substack URL. The Porch story and event pages never had this problem because their titles were always bare headings with nothing underneath them; nothing to fall through to. The video (and by structure, book) cards are different: `document.elementFromPoint()` at the exact center of the title text returned the bare `<h2>` itself, with no Sanity overlay anywhere in the hit path — so a real click lands on the title, bubbles up through nothing-in-particular, and the browser's native "this is inside a link" behavior wins, taking the visitor to YouTube instead of Sanity ever getting a chance to react.

Checked `featuredFiction`'s `renderBookCard()` too, since it's the other new listing-page type: its title was never wrapped in a link to begin with (only the Buy/Ebook buttons are their own separate anchors at the bottom) — so it doesn't have this failure mode. Only guild video needed the fix.

**Fix, approved by Rick before building:** a preview-only `renderVideoCardPreview()`, page-local to `the-work/videos/index.html`, used only when `isPreviewing()`. Same exact look as the real card, but the link only wraps the thumbnail and the "Watch →" line — the title is a bare heading again, with nothing underneath it to fall through to, matching the Porch/event pattern. A normal visitor still gets the original `renderVideoCard()` from `js/sanity.js`, byte-for-byte unchanged — confirmed directly on the live site after deploy (title still sits inside the real YouTube link for a cookie-less visit, exactly as before). `esc()` and `formatDate()` (`js/sanity.js`) are now exported so the preview-only renderer could reuse them instead of duplicating the logic — both already existed in that file, so exporting them added nothing to the bundle.

**Verified, not assumed:** on the test deployment, a real coordinate-accurate click (computed from the title's actual `getBoundingClientRect()`, not the accessibility tree's sometimes-stale reported position) landed on the title after the fix and produced no navigation and no new tab — the failure mode is now structurally impossible, since there's no link left to fall through to. Re-ran the full 64-page comparison after this fix, both against the test deployment and against live production post-merge: 0 mismatches both times.

**Still the same honest limitation as before:** whether Sanity's overlay itself successfully opens Studio on a real click (the *positive* outcome, not just "doesn't misfire") needs a real click inside an authenticated Presentation Tool session, same as Events/Bookshelf/Video's original build note above.

**Worth remembering:** this whole class of bug — a click-to-edit field that's also the descendant of a real link elsewhere in the app — is exactly the scenario Sanity's own docs cover with `data-sanity-edit-target`/edit groups. Didn't reach for that here since the simpler fix (don't nest the title in the link at all, preview-mode only) fully matched what already works elsewhere on this site. If a future card design can't avoid the nesting, that's the mechanism to look at next.

## 2026-08-29 (3) — Sanity access fully resolved

Sanity access fully resolved. Organization renamed to Southern Writers Guild. MJ and Grace invited as full Administrators, matching the partners' explicit choice to treat all three as equal, not tiered by role. Rick's personal account and the separate primalbeet project confirmed cleanly separated from the Guild's Sanity org. This closes the access gap that had been the actual point of the whole Visual Editing build — the tool has worked since 2026-08-28; as of today, the people it was built for can actually reach it.

## 2026-08-29 (2) — Click-to-edit fully working, confirmed live

Closes out the "Open, not done" item from the entry below: after shipping click-to-edit, Rick reproduced Presentation Tool's "Unable to connect" banner for real, on the live site, right after that deploy — the follow-up check this session deliberately deferred to. Two real, separate bugs, found and fixed in sequence, each confirmed against actual evidence before moving to the next:

**Bug 1 — the connection had nothing to attach to on the page Presentation Tool actually opens first.** Confirmed via Vercel's own logs across three separate live reproductions: every one resolved Presentation Tool's enable redirect to `/`, never to a Porch story path — because no document is selected yet on that very first load, and `previewUrl.initial` (Studio repo, unchanged by this session) points at the site root. Click-to-edit had only ever been wired into the Porch story page, so the homepage had nothing for the Comlink handshake to connect to. Fix: `js/visual-editing-bootstrap.js`, a real ES module built by its own Vite pass (`vite.bootstrap.config.js`, chained in `package.json`'s build script) since mixing a plain `.js` entry into the main HTML-multi-page `vite.config.js` broke Vite's HTML parsing for every other page — `js/nav.js` (already loaded on every page) dynamically imports its fixed-name output whenever the same non-secret preview cookie is present. Same safety property as before: normal visitors never request any of it.

**Bug 2 — that fix didn't actually clear the banner**, and the reason turned out to be unreachable by every tool available this session: the exact console error lives inside Presentation Tool's own UI, which is a same-origin-looking but actually isolated iframe nested two levels deep (top dashboard → Presentation Tool's own iframe → the site's iframe) — none of Claude Browser's, Claude in Chrome's, or any DevTools-adjacent tool's console/network readers could see into that middle frame, even with a live, reproducible failure sitting right there. Rick's own DevTools screenshot was the only way to actually see it: `Uncaught (in promise) ReferenceError: process is not defined`, thrown from inside the newly-built bootstrap chunk before `enableVisualEditing()` ever got the chance to attempt its connection. Cause: Vite's normal build replaces `process.env.NODE_ENV` with a literal automatically; its library-mode build (used for the bootstrap file specifically) does not, by design — a published library is meant to be re-bundled by whoever consumes it, but this file never is. Fix was a one-line `define` added to `vite.bootstrap.config.js`. Confirmed directly in the built output afterward, not assumed: the only unguarded `process.env.NODE_ENV` access is gone, and every remaining `process` reference in the bundle (styled-components' own feature checks) is already guarded with a `typeof` check first.

**Confirmed working end to end, for real, not inferred:** after the second fix, Presentation Tool connects cleanly on load (no banner, "Edit" toggle active) — watched this happen live, in Rick's own authenticated session via Claude in Chrome, not just reasoned about. Rick then opened an actual Porch story inside Presentation Tool and clicked its title: the Studio panel jumped straight to that field. That's the literal thing that was reported broken, now confirmed fixed by the person who reported it, on the real site.

**Full 65-page comparison re-run after both fixes**, against the same pre-visual-editing baseline as every check this thread: all 65 pages still match on status and title. Nothing else moved.

**Still not cleaned up, still just flagged:** the leftover branch-scoped `SANITY_PREVIEW_SECRET` override on `visual-editing-step2` and its CORS origin, both inert, both noted in the entry below — unchanged by this session's work, still there.

**Worth remembering about this specific class of bug going forward:** Presentation Tool's own connection-status UI lives in a frame that looks same-origin (`sanity.io`) but is genuinely isolated from it — screenshots from the person actually looking at the screen were the only way to see the real error text this session, and that's likely to stay true for any future debugging in this exact spot, not just this one incident.

## 2026-08-29

Click-to-edit built and shipped to `main` (commit `8c8b44e`), scoped to Porch stories, per the Going Pro plan's Step 9. `api/preview-story.js` now uses `@sanity/client` (drafts perspective, the existing `SANITY_PREVIEW_TOKEN`) instead of a raw `fetch()`, with stega encoding enabled only on that client — it only ever runs after the existing HttpOnly preview cookie check passes, so encoded output can't reach a request that hasn't already proven it's a live preview session. The story page dynamically imports `@sanity/visual-editing` only inside the existing `isPreviewing()` branch (same non-secret hint cookie already gating the draft fetch), confirmed via the build output that this puts the overlay code in its own lazy chunk, never referenced by a normal page load.

**Verified directly, not assumed, before and after the push:** loaded a story page in a real browser with no preview cookie, both on a throwaway branch deploy and again on the real live site after merging — zero stega characters anywhere in the rendered text, zero network request for the overlay code, zero console errors, cookie jar empty. Re-ran the full 65-page crawl (status/title/content hash) from before this work against the live site after deploy; every page matches once you discount the two lines that are expected to change on every build (Vite's per-build asset filenames) — confirmed by diffing the raw HTML directly, not just trusting the hash. One honest, non-security side effect: `stegaClean` (needed to keep the invisible markers out of `document.title` and image `alt` text, both flagged by Sanity's own docs as unsafe places for them to land) pulled a small shared runtime-helper chunk into the bundle every page already loads — about 5KB gzipped, invisible on screen, but a real small increase in what every visitor downloads.

**Tested with a throwaway secret, not the real one — real one confirmed impossible to test with.** `SANITY_PREVIEW_SECRET` and `SANITY_PREVIEW_TOKEN` are Vercel "Secret"-type variables: genuinely write-only, unreadable by anyone (including Rick) once saved, not just hidden from this session. To test the manual `?secret=` activation path at all, set a throwaway `SANITY_PREVIEW_SECRET` override scoped only to a test branch's Preview environment (`vercel env add ... --git-branch`), confirmed the whole flow works with it, then left that override in place rather than risk deleting the wrong one afterward — `vercel env rm` couldn't disambiguate it from the real value without a real risk of removing the production secret instead, since values can't be diffed to confirm which is which. **Not cleaned up, flagged rather than acted on:** the leftover branch-scoped override on `visual-editing-step2`, and the CORS origin added for that branch's preview alias. Both are inert (branch isn't getting pushed to again) but not removed.

**Genuine, not-just-a-testing-limitation finding:** nobody can produce a working `?secret=` link with the real value any more unless whoever generated it during the 2026-08-28 rotation saved it somewhere outside Vercel at the time. Worth knowing before assuming that manual link still works for sharing a preview outside Studio.

**Two click-to-edit contexts, one confirmed, one deliberately deferred:**
- The bare-link context (click outside Presentation Tool → open Studio in a new tab) — stega encoding and the overlay mounting were both confirmed directly; the actual "does clicking open a new tab" behavior could not be confirmed through this session's browser-automation tool. Synthetic clicks against the overlay (a closed-shadow-DOM custom element, `<sanity-visual-editing>`) produced inconsistent results across repeated attempts, most plausibly because this kind of programmatic click isn't always treated as a trusted user gesture for the purposes of opening a new tab or window — a tooling limitation, not a confirmed defect. Not independently verified by a human either, this session.
- The Presentation Tool context couldn't be tested pre-deploy at all: the Studio's `previewUrl.initial` (in the separate `southern-writers-guild` repo) is hardcoded to the real live domain, not any test/branch URL, and Sanity's own docs confirm there's no in-tool way to point Presentation Tool anywhere else without an env-var-driven config change and redeploy on the Studio side. **Decided this session (Rick's call):** push to `main` first, since everything with real visitor/safety impact was already fully verified clean, and check the actual click-to-edit experience in Sanity's Presentation Tool afterward as a fast follow-up rather than holding the whole deploy on it. **Open, not done:** that follow-up check.

## 2026-08-28 (8)

Rotated `SANITY_PREVIEW_SECRET` — the old value had been exposed in Studio's build output before the previous session's `@sanity/preview-url-secret` fix, so it was due for rotation on general hygiene grounds (flagged, not acted on, last session). New 256-bit value set in Vercel; the old one no longer works anywhere. Confirmed directly, not assumed, that env var changes don't take effect until a new deployment: right after updating the Vercel value, the live site still accepted the old secret and rejected the new one, then flipped the other way immediately after a fresh `vercel deploy --prod`. The Presentation Tool's own separate mechanism (Sanity's per-session secret, `@sanity/preview-url-secret`) is untouched by this — verified fresh via Vercel's logs (enable→302, preview-story→200) after the rotation, same pattern as before.

## 2026-08-28 (7)

Replaced the static preview secret baked into Studio's config with Sanity's own `@sanity/preview-url-secret` mechanism — that secret was ending up in Studio's public built JS bundle, which is exactly the pattern Sanity's docs warn against. Studio's `presentationTool` config no longer embeds anything; the Presentation Tool generates its own short-lived, per-session secret internally and `/api/preview` (site repo) validates it against the dataset using the library, reusing the same `SANITY_PREVIEW_TOKEN` already used for draft-fetching (no new token needed — it already has the read access this requires). Added `@sanity/client` and `@sanity/preview-url-secret` as real dependencies — first `package.json` this site repo has ever had, scoped to the `/api` serverless functions only, no effect on the static frontend's no-build-step design. The original manual `?secret=&slug=` path (the fixed server-side `SANITY_PREVIEW_SECRET`) still works unchanged, for direct testing and for sharing a preview link with someone outside Studio.

**Real finding while re-testing, not assumed:** Presentation Tool's actual enable request is a genuine cross-site iframe navigation, which makes the existing Partitioned-cookie logic (built last time, unchanged this time) actually kick in — meaning a session activated through Presentation Tool is invisible to a separate top-level browser tab by design (CHIPS partitioning, correct browser behavior). That broke the cross-tab verification trick used last round. Verified the real mechanism instead through Vercel's function logs (enable→302 immediately followed by preview-story→200) and by hitting `/api/preview-story` directly with the matching cookie value, which returned the actual draft content (`_originalId` confirms it's the draft). The always-available manual `?secret=&slug=` path — a normal top-level navigation, never partitioned — was used to re-verify the normal-visitor and exit-preview cases in a real browser the same way as before.

**Bundle inspection, the specific new check asked for:** pulled the actual deployed JS (both the local pre-upload build and the live-served file from swg-studio.sanity.studio — byte-identical) and grepped it directly. The config's `previewMode` shows up as literally `{enable:\`/api/preview\`,disable:\`/api/exit-preview\`}` — a bare path, nothing appended. Zero occurrences anywhere in the entire built output of the old secret value, the env var name, or even the word "secret" in any form.

**Not done, flagged rather than assumed:** the old static `SANITY_PREVIEW_SECRET` value was genuinely present in the Studio bundle before this fix (visible to anyone with Studio access, not the public internet) — it still works today via the manual path, unrotated. Rotating it is a reasonable hardening step but changes something Rick may already be relying on, so it wasn't done without asking.

## 2026-08-28 (6)

Sanity's Presentation Tool wired into the Studio repo (`southern-writers-guild`), scoped to porchStory only. Reused the existing `/api/preview` and `/api/exit-preview` endpoints rather than adopting Sanity's own `@sanity/preview-url-secret` handshake — Presentation Tool generates its own per-session secret and there's no supported way to make it send a chosen one instead, so the Studio config bakes the existing static secret directly into the enable URL (`SANITY_STUDIO_PREVIEW_SECRET`, in the Studio's local, gitignored `.env.local`, never committed — same "never commit a key" rule as the site's env vars) and ignores whatever secret Presentation Tool appends on its own. Added a `resolve.locations`/`resolve.mainDocuments` pair in `sanity.config.js` mapping `porchStory` to `/the-porch/{slug}/`.

Two real bugs found and fixed while testing this against the live embed, not just the manual `?secret=&slug=` flow from the original preview build:
- The preview cookies were `SameSite=Lax`, which is silently dropped on a cross-site iframe load (Presentation Tool embeds the site in an iframe from `sanity.io` — not a top-level navigation, so Lax's usual top-level-navigation exception doesn't apply). Switched to `SameSite=None; Secure`, with Safari's CHIPS `Partitioned` attribute added only when the request actually looks like it's coming from that iframe (`sec-fetch-dest`/`sec-fetch-site` headers) — same pattern Sanity's own docs use.
- `/api/preview` 404'd on Presentation Tool's very first call (no document selected yet, pathname `/`), which isn't a Porch story path — and Presentation Tool retries the enable call in a loop when it fails, which hung the whole preview pane before an editor ever picked anything. Now `/` is accepted as a valid, harmless redirect target alongside `/the-porch/{slug}/`; the actual draft-content route (`/api/preview-story`) still only ever serves `porchStory` documents regardless, so this doesn't widen what's actually previewable.

Tested for real, not just by reading code: opened the Studio in a real logged-in Chrome session, selected Gracelets via Structure's "Used on 2 pages" link (confirmed the resolver's output directly — `/the-porch/gracelets/`), watched the address bar inside Presentation resolve to the real live URL with real rendered content behind it. Patched the draft title without publishing, and — since Presentation's own click-to-edit connection never established (expected: `@sanity/visual-editing` overlays were never installed, out of scope here, and its absence surfaces as a permanent "Unable to connect" banner over the iframe that made screenshotting the pane itself unreliable) — confirmed the edit landed by opening the exact resolved URL in a second tab in the *same* browser: it showed the unpublished title. A `curl` with no cookie against the same URL, and a fresh visit after `/api/exit-preview`, both still showed the real published title. Test draft discarded afterward.

**Open, not decided:** click-to-edit overlays (`@sanity/visual-editing`, stega encoding) were not built — this task was the enable/disable/resolver wiring only. That's the next piece if full visual editing (Step 9) gets prioritized.

## 2026-08-28 (5)

Private preview mode built and tested, scoped to Porch stories only — the test case ahead of extending it elsewhere. `/api/preview` (validates a secret, sets a cookie, redirects to the story), `/api/exit-preview` (clears it), `/api/preview-story` (server-side only, fetches the draft with a privileged token when the cookie checks out). Two new Vercel env vars, both server-side only, same pattern as `KIT_API_KEY`: `SANITY_PREVIEW_SECRET` (the activation secret) and `SANITY_PREVIEW_TOKEN` (a Sanity "viewer" role robot token — read access, drafts included, nothing else). Tested end to end: edited a real story's draft without publishing, confirmed the draft text appeared only in the browser session that had visited `/api/preview` with the right secret, confirmed a cookie-less request (and a request with a forged cookie) got a plain 404 with no hint, confirmed `/api/exit-preview` fully reverted the session to published content.

**Naming note for later:** `SANITY_PREVIEW_TOKEN` is scoped to this narrow feature. If Step 9 (real visual editing, per the Going Pro plan) gets built later using Sanity's official `@sanity/preview-url-secret` pattern, that work will likely want its own token following Sanity's own convention (`SANITY_API_READ_TOKEN`) and a proper Presentation Tool handshake — this token and cookie scheme is a simpler hand-rolled version built for this one test case, not meant to be the permanent mechanism Step 9 builds on. Revisit naming then rather than assuming reuse.

## 2026-08-28 (4)

Real founder addresses confirmed: beau@ (Rick), hank@ (MJ), gray@ (Grace) — using pen names, not real names, consistent with the Guild's naming convention. Mailboxes exist; Google Accounts for these three still need creating, one-time process each, verified against current Google signup flow. jp@ confirmed as the anchor identity for both the YouTube Brand Account and the shared Guild Drive — one identity, multiple jobs, by design.

## 2026-08-28 (3)

Step 8 phases 1-2 complete. Videos and Bookshelf pages rewired to existing Sanity fetch functions (no new schema). New siteSettings singleton added (contactEmail field), House page's two hardcoded addresses now pull from one Sanity document, hardcoded value kept as fallback if the fetch fails. Side effect: the guildVideo showOnHomepage toggle, which existed in a prior unpushed commit, went live in the same deploy — homepage "Right Now" section now actually includes Guild Videos as designed, closing a gap that had existed silently. Phase 3 (Writers page + subpages — needs a new schema type, paused on a real design decision, not started) and Phase 4 (House mission/beliefs copy, guidelines page — low priority) remain open.

## 2026-08-28 (2)

Step 0 partially cleared: LLC formed and confirmed real; business bank account still under evaluation, not final. This unblocks anything requiring the LLC to legally exist (Step 3 named explicitly in the plan), but not anything requiring finalized bank details. Step 4 confirmed not yet in place — addresses decided, not provisioned in IONOS, no Google Accounts created. Decision made this session: proceed with Step 4 work now rather than wait, since it was already sanctioned as quiet prep independent of Step 0's exact status.

## 2026-08-28

Step 0 (LLC + bank account) confirmed cleared by Rick. Step 4 (founder email identities) confirmed not yet in place — this is now the live prerequisite before any real Creator Workstation build, since its "your account" tiles are literally these accounts. — Correction, same day: this entry overstated Step 0 as fully cleared. See the "2026-08-28 (2)" entry above for the accurate status: LLC formed and confirmed, business bank account still under evaluation, not final.

---

## 2026-08-30 (2)

Ruled out stale software as the cause of Guild Video/Bookshelf's "No matching documents" problem — checked directly, not assumed, per Rick's explicit request.

Site side (`swg-site`): `@sanity/client`, `@sanity/visual-editing`, `@sanity/comlink`, `@sanity/presentation-comlink` — all at the latest version available on npm, no gap.

Studio side (`southern-writers-guild`): `sanity.cli.js` has `autoUpdates: true`, which means the version pinned in `package.json` (5.23.0) is not what's actually served — the deployed Studio resolves its real version dynamically from Sanity's own CDN at request time, constrained only by the semver range. Confirmed this empirically, not from documentation alone: fetched the deployed Studio's real HTML and import map, followed the resolved module URL, and read the `x-resolved-version` response header directly — the live, actually-running version is **5.31.1**, roughly 8 minor versions ahead of the pinned 5.23.0, not behind it.

Pulled the real `packages/sanity/CHANGELOG.md` from `sanity-io/sanity` on GitHub (not doc-site slug guessing, which mostly failed) and grepped the exact range between 5.23.0 and 5.31.2 for anything presentation/overlay/comlink-related. Two hits looked plausible; pulled the full PR text for both rather than trusting the one-line summary:

- `fix(presentation): ChildLink now renders missing docs` (#12958) — fixes the *incoming references* inspector panel specifically, unrelated to the "Documents on this page" panel.
- `fix(presentation): handle null client.fetch results in PostMessageSchema` (#12904) — fixes a crash when a document doesn't exist at all on the active perspective; our documents exist and render correctly, so this doesn't match either.

Also checked `@sanity/presentation-comlink` specifically (a more specific package than generic `@sanity/comlink`, called out in the same changelog range): site is at 2.2.3 (npm latest), Studio's local `node_modules` has 2.0.1 — a real gap, but moot for the same `autoUpdates` reason above; the deployed Studio doesn't run from local `node_modules`.

Confirmed `js/visual-editing-bootstrap.js`'s call to `enableVisualEditing({ history })` matches the installed `@sanity/visual-editing@6.1.1` type definitions exactly — no API mismatch.

**Conclusion:** stale/mismatched software is ruled out as the cause. The "No matching documents" problem is real and still open — the two live leads remain the DOM-nesting-depth difference between working and non-working pages (found earlier), and the cross-origin-blocked Comlink channel that couldn't be inspected directly. Confirming whether Guild Video/Bookshelf titles actually jump into Studio still needs a real human click inside an authenticated Presentation Tool session — already established as untestable by automation (the `window.open` control test proved that approach can't tell a working page from a broken one).

## 2026-08-19 — Session close-out

Cowork (this kind of session) and Claude Code are separate, isolated environments by design, not a bug — Cowork's container has no local credentials, which is why a git push needs a Claude Code handoff rather than happening directly. This repo, read by both, is the real shared channel between them; there's no live link beyond it.

**Checkpoint for any future Claude Code session:** should show folder `swg-site`, branch `main`, before anything gets typed. A different project name showing up there — "rick-west-site" did, this session — is the signal to stop before proceeding.

**Unresolved, worth a real look if it comes up again:** what "rick-west-site" actually is. Confirmed as a genuinely separate project from SWG (its real GitHub remote is `swg-site-v12`, not that name), and there are files named `rick-west*.jsx` inside the `primalbeet` folder — likely Rick's personal site project. Not confirmed: whether anything from a past session's handoff accidentally touched it instead of SWG.

Commit `019a856` (this log's creation, the CLAUDE.md continuity rule, and `the-commitment-behind-the-bird.md`) is confirmed pushed to `swg-site-v12` on GitHub — verified directly via Claude Code, not assumed.

No separate summary document needed for the next thread. Reading this file and `CLAUDE.md` — automatic once this folder is connected — covers it.

## 2026-08-19

**Continuity process, decided.** This file exists because relying on a Cowork project's cached/uploaded files as memory failed — that cache goes stale (last synced 2026-08-11, missed everything built after). This repo, connected live, is the only source treated as current. Going forward: settled decisions and standing rules go in `CLAUDE.md`; everything else that matters but isn't settled goes here, written without being asked, at natural stopping points in a conversation — not something Rick has to remember to request.

**"It's All About the Bird" — reader-facing essay, done.** Final version written by Rick, saved as `the-commitment-behind-the-bird.md` in that session's outputs (not yet in this repo — worth moving in if it's going on the site). Governing metaphor: harbor vs. open sea, sailing a self-built corsair, not a warship, not stolen. The unholy trio (all three founders) as the crew. Thesis: ownership isn't the point, it's the price — what it buys is freedom of shape, since every platform-of-one-shape (Substack, YouTube) forces whatever you make into its own template regardless of fit. Jean-Paul closes the piece on the bow of the ship. Deliberately excludes: the Porch/Kitchen Table split, any specific real-world example (Rick's personal Albania piece was ruled out as off-topic for a Guild piece), and any CTA — the CTA lives elsewhere on the page, in a different format, not in the essay text.

**Audio pages — Open, exploratory only, nothing decided.** Partners asked for a page each (three individuals) plus one group page focused on audio. Rationale for the fourth/group page, resolved during discussion: it's for ensemble "skits" — Garrison Keillor-style pieces performed by all three together, which have no natural home on any individual's page. Structural lean, not decided: one Sanity-driven template (`/listen/[persona]`) rather than four hand-built pages, since each partner will self-serve edit their own entry directly in Sanity — Rick is not the bottleneck, each partner touches Sanity at most twice (their own page, and the group page if voted in). QR codes framed as a handoff, not a doorway — the scan should get someone listening in one tap, not route them through navigation. Real podcast distribution (Apple Podcasts, Spotify) needs a dedicated podcast host for RSS; a page with an embedded player is not the same thing as podcast distribution. Video hosting, if it happens: YouTube (free, but subject to advertiser-friendly content policy and algorithmic reach limits — a real concern given Rick's content doesn't always stay in "approved" territory on his personal work, though the Guild's own material is expected to stay within YouTube's guidelines) versus Cloudflare Stream/Mux (paid, no content-policy layer, full control). Nothing built. Nothing scheduled.

**Grace, factual correction for future reference.** She appears on camera regularly — voice and face are not new exposure. What she actually protects: legal name, street address, city of residence. Don't assume audio/video appearances are a privacy risk for her without cause.

**Vercel tile labeled "Shared login" in the workstation concept preview — flagged, not resolved.** Earlier reasoning had Vercel in the same bucket as GitHub/Sanity/Canva (individual Google sign-in, no vault needed). Worth checking whether that's a real distinction (e.g., not on a Team plan) or the mockup just hasn't caught up to the plan.

---
